이건 아예 **미래의 내가 서비스 하나를 보고 컬럼까지 내려가는 절차서**로 고정해두는 게 좋음. 핵심은 `Service → 바로 컬럼`으로 점프하지 않는 거다. 그 사이에 **행동 → DB행동 → 데이터 → 저장할 원자값**을 한 층씩 밟아야 함. 중간계단 생략하면 다시 숲에서 나무껍질 찾는 꼴 남.

# 서비스에서 컬럼까지 쪼개는 방법

```text
Service
업무 하나
↓
업무를 이루는 판단/행동
↓
DAO
DB에서 해야 하는 행동
↓
DTO
그 행동에 필요한 데이터
↓
원자 데이터
더 쪼갤 수 없는 하나의 사실
↓
COLUMN
DB가 실제로 기억해야 하는 값
```

한 문장으로 외우면:

```text
Service = 무슨 업무를 하는가
DAO     = DB에서 무슨 행동을 하는가
DTO     = 그 행동에 무슨 데이터가 필요한가
Column  = 그중 DB가 기억해야 하는 가장 작은 사실은 무엇인가
```

---

## 1. Service를 먼저 한 문장으로 만든다

처음부터 컬럼 생각하지 않는다.

예를 들어 근태라면:

```text
직원의 하루 근무기록을 확인하고
실제 근무시간과 연장·야간근무시간을 계산하여
근태를 저장한다.
```

여기서는 아직:

```text
clock_in
overtime_hours
```

같은 생각 안 함.

먼저 **이 업무가 실제로 무슨 일을 하는지**만 본다.

---

## 2. Service 문장을 행동 단위로 쪼갠다

위 문장을 순서대로 자른다.

```text
[근태 Service]

근로계약을 확인한다
↓
근무날짜를 확인한다
↓
출근시간을 확인한다
↓
퇴근시간을 확인한다
↓
휴게시간을 확인한다
↓
평일/휴일을 판단한다
↓
실근무시간을 계산한다
↓
연장근무시간을 계산한다
↓
야간근무시간을 계산한다
↓
근태상태를 결정한다
↓
근태기록을 저장한다
```

여기서 중요한 구분:

```text
확인한다
조회한다
저장한다
수정한다
삭제한다
→ DB 행동 후보

판단한다
계산한다
검증한다
결정한다
→ Service 책임
```

즉 **Service에서 계산한답시고 계산용 컬럼이 전부 나오는 건 아님.**

---

## 3. 각 Service 행동에서 DAO 행동을 뽑는다

Service가 DB에 부탁해야 하는 일만 골라낸다.

예:

```text
근로계약을 확인한다
→ 현재 근로계약을 조회한다

해당 날짜의 기존 근태를 확인한다
→ 날짜별 근태를 조회한다

계산한 근태를 남긴다
→ 근태를 등록한다

기존 근태를 고친다
→ 근태를 수정한다
```

그러면 DAO는:

```text
[EmployeeAttendanceDao]

특정 날짜 근태 조회
특정 월 근태 조회
근태 등록
근태 수정
근태 삭제
```

이렇게 됨.

### 여기서 기억

```text
Service
"하루 근태를 처리한다"

↓ 쪼개기

DAO
"계약 조회"
"근태 조회"
"근태 저장"
```

즉:

> **Service는 업무를 쪼개고, DAO는 그 업무에서 필요한 DB 행동만 뽑는다.**

---

## 4. DAO 행동마다 "뭐가 필요하지?"를 묻는다

이제부터 데이터가 나오기 시작함.

예를 들어:

```text
특정 날짜 근태를 조회한다
```

그러면 묻는다.

```text
누구의 근태?
→ contract_no

어느 날?
→ work_date
```

따라서:

```text
contract_no + work_date
→ 특정 날짜 근태 조회
```

또:

```text
근태를 등록한다
```

라고 하면:

```text
어떤 계약?
어느 날짜?
평일인가 휴일인가?
몇 시 출근?
몇 시 퇴근?
몇 분 휴게?
연장 몇 시간?
야간 몇 시간?
어떤 근태상태?
```

가 필요함.

이게 DTO 후보가 됨.

---

# 5. 질문의 답을 "원자 데이터"로 만든다

여기가 제일 중요함.

질문:

```text
몇 시에 출근했는가?
```

답:

```text
출근시간
```

질문:

```text
얼마나 연장근무했는가?
```

답:

```text
연장근무시간
```

질문:

```text
현재 근태가 어떤 종류인가?
```

답:

```text
근태구분
```

이런 식으로 **더 이상 의미 있게 쪼갤 수 없는 하나의 값**까지 내려간다.

```text
근태
↓
(연장) 근무
↓
연장 근무시간
```

여기서 마지막:

```text
연장 근무시간
```

이 컬럼 후보임.

---

# 6. 원자 데이터를 DTO 필드로 만든다

사람말을 프로그램 데이터로 번역한다.

```text
근로계약 번호
→ contractNo

근무 날짜
→ workDate

출근 시각
→ clockIn

퇴근 시각
→ clockOut

휴게 시간
→ breakMinutes

근무일 구분
→ workDayType

연장 근무시간
→ overtimeHours

야간 근무시간
→ nightHours

근태 구분
→ attendanceType
```

이게 DTO.

```java
EmployeeAttendanceDto

empAttendanceNo
contractNo
workDate
clockIn
clockOut
breakMinutes
attendanceType
workDayType
overtimeHours
nightHours
```

---

# 7. DTO 필드라고 전부 컬럼으로 만들지는 않는다

여기서 반드시 한 번 필터링한다.

각 데이터마다 이렇게 판단:

```text
이건 "행동"인가 "값"인가?
        ↓

값이라면
DB가 나중에도 기억해야 하는가?
        ↓

기억해야 한다면
다른 저장값만으로 쉽게 다시 만들 수 있는가?
        ↓

그래도 보존할 이유가 있는가?
```

예를 들어:

```text
근태를 계산한다
→ 행동
→ 컬럼 X
```

```text
clock_in
→ 원본 사실
→ 저장 O
```

```text
clock_out
→ 원본 사실
→ 저장 O
```

```text
실근로시간
→ clock_in/out/break로 다시 계산 가능
→ 저장하지 않을 수 있음
```

```text
overtime_hours
→ 계산값
→ 하지만 월 급여 계산에 사용하고
  당시 계산 결과를 보존할 필요가 있음
→ 저장 O
```

즉:

> **계산 가능 = 무조건 저장 안 함이 아님.**

"그 당시 결과를 다시 재현해야 하는가?"도 봐야 함.

급여는 특히 계산 결과 스냅샷이 중요함.

---

# 8. 저장하기로 한 원자 데이터를 컬럼으로 바꾼다

이제서야 컬럼명.

```text
근무날짜
→ work_date

출근시각
→ clock_in

퇴근시각
→ clock_out

휴게시간
→ break_minutes

평일/휴일
→ work_day_type

연장근무시간
→ overtime_hours

야간근무시간
→ night_hours

근태구분
→ attendance_type
```

즉:

```text
Service
하루 근태 처리

↓

DAO
근태 조회 / 저장

↓

DTO
근무날짜
출퇴근
휴게
근무일구분
연장시간
야간시간
근태구분

↓

Column
work_date
clock_in
clock_out
break_minutes
work_day_type
overtime_hours
night_hours
attendance_type
```

이렇게 내려오는 거임.

---

# 9. 마지막에 PK / FK / UNIQUE를 별도로 도출한다

일반 컬럼을 뽑은 다음 **관계용 컬럼**을 확인한다.

### PK 질문

```text
이 기록 한 건 자체를 어떻게 구별하지?
```

예:

```text
직원근태 한 건
→ emp_attendance_no
```

### FK 질문

```text
이 기록은 누구/무엇에 속하지?
```

예:

```text
직원근태는 어떤 계약에 속함?
→ contract_no
```

### UNIQUE 질문

```text
업무적으로 어떤 조합이 두 번 생기면 안 되지?
```

예:

```text
같은 계약
+
같은 근무날짜

→ 두 근태가 존재하면 이상함
```

따라서:

```sql
UNIQUE(contract_no, work_date)
```

급여라면:

```text
같은 계약
+
같은 연도
+
같은 월

→ 급여 두 개 존재하면 안 됨
```

따라서:

```sql
UNIQUE(
    contract_no,
    payroll_year,
    payroll_month
)
```

---

# 쪼개기할 때 사용하는 고정 질문

막히면 컬럼명을 억지로 생각하지 말고 아래 질문을 던진다.

```text
누구의 것인가?
→ 식별자 / FK

무엇의 것인가?
→ FK

언제인가?
→ 날짜 / 시각

어떤 종류인가?
→ TYPE

어떤 상태인가?
→ STATUS

얼마인가?
→ AMOUNT / WAGE / PRICE

얼마나 했는가?
→ HOURS / MINUTES / QTY

몇 %인가?
→ RATE

언제부터 언제까지인가?
→ START / END

왜 이런 결과인가?
→ NOTE / REASON

이 기록 자체는 몇 번인가?
→ PK
```

이 질문에 대한 **답 하나가 대체로 컬럼 하나**가 된다.

---

# 예제 2: 급여 지급 계산근거

Service부터 시작.

```text
[지급 계산 Service]

지급항목을 확인한다
↓
계산 기준금액을 확인한다
↓
적용시간을 확인한다
↓
지급금액을 계산한다
↓
계산근거를 남긴다
```

DAO로 내려감.

```text
지급항목 조회
계산근거 등록
계산근거 조회
```

DAO가 계산근거를 등록하려면 뭐가 필요한지 질문.

```text
어떤 지급항목의 계산인가?
→ 지급항목번호

무엇을 기준으로 계산했는가?
→ 기준금액

얼마나 적용했는가?
→ 적용시간

결과가 얼마인가?
→ 계산된 지급금액

왜 이 금액인가?
→ 계산비고
```

DTO:

```text
payrollEarningNo
baseAmount
workHours
earnCalculatedAmount
earnCalcNote
```

DB 컬럼:

```text
payroll_earning_no
base_amount
work_hours
earn_calculated_amount
earn_calc_note
```

그리고:

```text
이 계산근거 자체를 구별?
→ earning_calc_no
```

완성.

---

# 예제 3: 공제 계산근거

Service:

```text
공제항목 확인
↓
공제 기준금액 결정
↓
기준연도 확인
↓
계산방식 결정
↓
요율 또는 정액 적용
↓
공제금액 계산
↓
계산근거 저장
```

질문으로 변환:

```text
어떤 공제인가?
→ deduction_no

무슨 금액을 기준으로 했나?
→ base_amount

어느 연도 기준인가?
→ deduction_basis_year

몇 % 적용했나?
→ deduction_rate

정액이면 얼마인가?
→ fixed_deduction_amount

결과 얼마를 뺐나?
→ deduction_calculated_amount

왜 이렇게 계산됐나?
→ deduction_calc_note
```

이러면 컬럼이 그냥 나옴.

중요한 건 `deduction_rate`를 머리에서 창조한 게 아니라,

```text
공제한다
↓
얼마를?
↓
어떤 방식으로?
↓
요율로
↓
몇 %?
```

까지 **질문으로 내려가서 나온 것**이라는 점.

---

# 미래의 내가 막혔을 때 쓰는 순서

이것만 저장해놔도 됨.

```text
[1]
Service를 한 문장으로 쓴다.
"이 서비스는 무엇을 완성하는가?"

↓

[2]
그 업무를 시간순 행동으로 쪼갠다.

조회
확인
판단
계산
저장
상태변경

↓

[3]
그중 DB가 필요한 행동만 DAO로 뽑는다.

조회
등록
수정
삭제
존재확인
이력조회

↓

[4]
각 DAO 행동에 필요한 정보를 질문한다.

누구?
언제?
무엇?
무슨 종류?
얼마?
얼마나?
무슨 상태?
왜?

↓

[5]
질문의 답을 더 쪼갤 수 없는
원자 데이터로 만든다.

↓

[6]
원자 데이터를 DTO 필드로 만든다.

↓

[7]
각 DTO 데이터가 정말 DB에 저장되어야 하는지 판단한다.

원본 사실?
계산값?
임시값?
다시 계산 가능한가?
당시 결과를 보존해야 하는가?

↓

[8]
저장하기로 한 값만 컬럼으로 만든다.

↓

[9]
마지막으로 관계를 본다.

한 행 자체 식별?
→ PK

무엇에 속함?
→ FK

어떤 조합이 중복되면 안 됨?
→ UNIQUE

값 범위가 제한됨?
→ CHECK

필수임?
→ NOT NULL
```

# 그리고 역검증

컬럼을 다 만들고 나면 **반대로 올라가본다.**

```text
COLUMN
↓
DTO에서 의미가 있는가?
↓
DAO가 실제로 사용하는가?
↓
Service 업무에서 필요한가?
```

예를 들어 갑자기:

```text
special_code
```

라는 컬럼이 있는데

```text
이 값 어느 DTO에서 쓰지?
→ 모르겠음

어느 DAO가 필요하지?
→ 모르겠음

어느 Service 업무에 필요하지?
→ 모르겠음
```

이면 높은 확률로 **쓸데없이 생긴 컬럼**임.

반대로 Service에:

```text
계약 상태를 판단한다
```

가 있는데 DB 어디에도 계약 상태가 없다면:

```text
Service
↓
필요한 원자 데이터 누락
↓
contract_status 필요
```

라고 역으로 발견 가능함.

---

## 진짜 핵심 공식

미래의 너는 이것만 기억해.

```text
업무를 쪼갠다
        ↓
행동을 찾는다

행동을 쪼갠다
        ↓
필요한 데이터를 찾는다

데이터를 쪼갠다
        ↓
원자 사실을 찾는다

원자 사실 중
기억할 것을 고른다
        ↓
컬럼이 된다
```

그러니까 **컬럼을 직접 "생각해내려고" 하지 마.**

```text
"무슨 컬럼 만들지?"
```

라고 물으면 막힘.

대신:

```text
이 업무를 하려면
DB가 나중에도 기억하고 있어야 하는 사실이 뭐지?
```

라고 물어.

그 답이 컬럼임.

네 쪼개기 방법도 여기까지 확장하면 충분히 컬럼 도출 도구로 쓸 수 있다. 원래 네가 만든 `Service → DAO → DTO` 사고법에서 마지막에 **`DTO → 저장할 원자 사실 → Column`** 한 층만 붙이면 완성임. 이제 숲에서 나무를 보는 정도가 아니라 나뭇잎까지 내려갈 수 있음. 더 쪼개면 세포생물학이니까 거기서 멈추면 된다.
