ㅇㅋ, 네가 원하는 건 **“근태 = `(휴일) 출근 → (연장·야간) 근무 → 퇴근`”처럼 앞의 수식어까지 괄호로 쪼개서** 9개 테이블 전부 사람말 흐름으로 만드는 거지.

그럼 DDL 기준으로 이렇게 보면 제일 깔끔함.

```text
① employment_contract 근로계약

(월급 / 시급 / 일급) 급여
        ↓
(계약상) 기준임금
        ↓
(1일) 소정근로시간
        ↓
(주) 소정근로시간
        ↓
(매월) 급여지급일
        ↓
(시작 / 종료) 계약기간
        ↓
(예정 / 진행중 / 종료) 계약상태
```

```text
② employee_attendance 직원근태

(평일 / 휴일) 근무
        ↓
출근
        ↓
(휴게) 시간
        ↓
(연장 / 야간) 근무
        ↓
퇴근
        ↓
(정상 / 결근 / 유급휴가 / 무급휴가) 근태
```

네가 말한 표현으로 더 압축하면:

```text
(평일/휴일) 출근
→ (연장/야간) 근무
→ 퇴근
→ (정상/결근/유급/무급) 처리
```

```text
③ payroll_attendance 급여반영근태

(해당 월) 근태
        ↓
(급여 대상) 근태 선택
        ↓
(해당 급여) 근태 반영
```

즉 얘는 진짜 간단하게:

```text
(월별) 근태
→ (급여) 반영
```

`payroll_no + emp_attendance_no`가 바로 이 의미임.

```text
④ payroll 급여

(근로계약) 급여
        ↓
(연 / 월) 산정기간
        ↓
(산정중) 급여
        ↓
(확정) 급여
        ↓
(지급완료) 급여
```

압축:

```text
(월별) 급여 생성
→ (산정중) 계산
→ (확정) 급여
→ (지급완료) 급여
```

```text
⑤ payroll_earning 지급항목

(기본급 / 수당 / 기타) 지급
        ↓
(기본급 / 주휴 / 연장 / 야간 ...) 항목
```

즉:

```text
(기본급) 지급
(주휴) 수당
(연장) 수당
(야간) 수당
```

여기는 네가 원하는 식으로 쪼개면 특히 보기 좋음.

```text
(기본) 급
(주휴) 수당
(연장) 수당
(야간) 수당
```

---

```text
⑥ payroll_earning_calc 지급계산근거

(계산) 기준금액
        ↓
(적용) 근무시간
        ↓
(계산된) 지급금액
        ↓
(계산) 비고
```

실제 예시는:

```text
(연장) 수당
    ↓
(기준) 시급
    ↓
(연장) 근무시간
    ↓
(최종) 지급금액
```

또는:

```text
(야간) 수당
→ (기준) 시급
→ (야간) 시간
→ (계산) 금액
```

---

```text
⑦ payroll_deduction 공제항목

(국민) 연금
(건강) 보험
(장기요양) 보험
(고용) 보험
(소득) 세
(지방소득) 세
(기타) 공제
```

이 테이블은 오히려 앞단어 쪼개기가 아주 잘 됨.

```text
(국민) 연금
(건강) 보험
(고용) 보험
(소득) 세금
```

---

```text
⑧ payroll_deduction_calc 공제계산근거

(공제) 기준금액
        ↓
(공제) 기준연도
        ↓
(적용) 요율
     또는
(고정) 공제금액
        ↓
(계산된) 공제금액
        ↓
(계산) 비고
```

실제 흐름:

```text
(건강) 보험
→ (공제) 기준금액
→ (해당연도) 요율
→ (최종) 공제금액
```

또는 소득세라면:

```text
(소득) 세금
→ (과세) 기준금액
→ (간이세액표) 기준
→ (최종) 공제금액
```

---

```text
⑨ payroll_payment 급여지급

(확정된) 급여
        ↓
(실제) 지급금액
        ↓
(계좌이체 등) 지급방법
        ↓
(실제) 지급일시
        ↓
(지급 / 취소) 처리
        ↓
(지급) 비고
```

압축하면:

```text
(실제) 지급
→ (계좌이체) 처리
→ (지급/취소) 이력
```

---

그리고 **네가 종이에 그릴 때는 이 정도까지 압축해서 적는 게 제일 좋음.**

```text
[근로계약]
(월급/시급/일급) 계약
→ (1일/주) 근로시간
→ (예정/진행/종료) 계약

        ↓

[직원근태]
(평일/휴일) 출근
→ (연장/야간) 근무
→ 퇴근
→ (정상/결근/유급/무급) 근태

        ↓

[급여반영근태]
(월별) 근태
→ (급여) 반영

        ↓

[급여]
(월별) 생성
→ (산정중) 계산
→ (확정) 급여

        ↓
   ┌──────────────┐
   ↓              ↓

[지급항목]       [공제항목]
(기본)급         (국민)연금
(주휴)수당       (건강)보험
(연장)수당       (고용)보험
(야간)수당       (소득)세

   ↓              ↓

[지급계산]       [공제계산]
(기준)시급       (기준)금액
(적용)시간       (적용)요율
(최종)금액       (최종)금액

   └──────┬───────┘
          ↓

[급여지급]
(실제) 지급
→ (계좌이체) 방법
→ (지급/취소) 이력
```

**이 버전이 네 손그림에 옮기기 제일 좋다.**  
특히 `(연장)수당`, `(야간)수당`, `(건강)보험`, `(소득)세`처럼 **괄호 안을 “종류/조건”, 뒤쪽 단어를 “행위·대상”으로 통일**하면 그림만 봐도 뭐 하는 테이블인지 바로 읽힘. 이제 외계어였던 테이블들이 슬슬 인간 언어로 귀화 중이다.
