# 스케줄, 근태 설계 최종본

이 문서는 근태 설계 **1단계 ~ 10단계**를 현재까지 확정된 기준으로 다시 정리한 문서다.

기존 문서에 섞여 있던 중간 설계 중 아래 내용은 폐기한다.

```text
- 근무 스케줄 삭제 기능
- 스케줄 수정 시 schedule_day_type만 수정한다는 규칙
- attendanceStatus 필드/개념
- break_start / break_end
- 휴게중 상태
```

현재 핵심 흐름은 그대로다.

```text
근로계약
→ 근무 스케줄
→ 실제 근태
→ 급여반영근태
→ 급여
```

그리고 계층 책임도 그대로 유지한다.

```text
RequestVO / ResponseVO
= 외부/화면과 통신

DTO
= DB 테이블 단위 실제 데이터

DAO
= DB 행동

Service
= 판단 / 검증 / 계산 / 조립

Controller
= HTTP 요청 수신 / 인증정보 전달 / Service 호출 / Response 반환
```

---

# 1단계. 현실의 근태 업무 정의

## 1. 근무 스케줄

`employee_work_schedule`은 실제 근태가 아니라 **예정**이다.

```text
employee_work_schedule
= 직원 X가 날짜 Y에 근무 예정인지 휴무 예정인지
```

값:

```text
schedule_day_type
- 근무일
- 휴무일
```

`schedule_date`는 Oracle `TIMESTAMP`를 유지하고 Java에서는 `LocalDateTime`을 사용한다.

단, 하루 단위 일정이므로 Service에서 반드시 날짜 시작 시각으로 정규화한다.

```java
scheduleDate = scheduleDate.toLocalDate().atStartOfDay();
```

따라서 같은 직원에게 같은 날짜 스케줄은 하나만 존재한다.

```text
UNIQUE(employee_no, schedule_date)
```

---

## 2. 실제 근태

`employee_attendance`는 예정이 아니라 **실제로 발생한 하루 근태 결과**다.

한 행의 의미:

> 특정 근무일정과 특정 근로계약 아래에서 특정 날짜에 실제로 발생한 하루 근태 결과

근태 생성 시 핵심 귀속값:

```text
work_schedule_no
= 어떤 예정에서 발생했는가

contract_no
= 어떤 근로조건 아래 발생했는가

work_date
= 어느 날의 근태인가
```

---

## 3. 정상 출근

```text
직원이 출근 시도
↓
로그인한 직원 확인
↓
오늘 날짜 확정
↓
오늘 본인의 스케줄 조회
↓
스케줄 없음
→ 출근 불가
↓
오늘 적용 계약 조회
↓
해당 스케줄에 이미 근태가 있는지 확인
↓
이미 근태 있음
→ 신규 출근 불가
↓
현재 열려 있는 다른 근태가 있는지 확인
↓
있음
→ 중복 출근 불가
↓
근태 생성
```

스케줄이 `근무일`이면:

```text
attendance_type = 정상
work_day_type = 근무일
```

스케줄이 `휴무일`이어도 출근 자체는 가능하다.

```text
attendance_type = 정상
work_day_type = 휴무일
```

이 조합은:

```text
원래 휴무일이지만 실제로 근무함
```

이라는 사실을 보존한다.

---

## 4. 정상 퇴근

퇴근은 단순히 `clock_out` 하나 저장하는 행동이 아니다.

```text
직원이 퇴근 시도
↓
현재 열려 있는 본인 정상 근태 조회
↓
clock_in 존재 확인
↓
clock_out 미존재 확인
↓
현재 시각을 clock_out으로 확정
↓
적용된 break_minutes 확인
↓
실제 근무시간 계산
↓
계약의 daily_work_hours 확인
↓
overtime_hours 계산
↓
night_hours 계산
↓
저장
```

중요한 점은 **오늘 스케줄을 다시 찾는 방식으로 퇴근 대상을 찾지 않는 것**이다.

야간근무가 가능하기 때문이다.

예:

```text
work_date = 2026-08-31
clock_in  = 2026-08-31 22:00
clock_out = 2026-09-01 06:00
```

따라서 퇴근 대상은:

```text
attendance_type = 정상
clock_in is not null
clock_out is null
```

인 현재 열린 근태를 직원 기준으로 조회한다.

---

## 5. 미출근과 결근은 다르다

```text
schedule_day_type = 근무일
employee_attendance 없음
```

이면 현실 상태는 미출근이다.

하지만:

```text
미출근 != 결근
```

이다.

`attendance_type = 미출근` 같은 DB 값을 만들지 않는다.

관리자가 실제로 결근을 확정했을 때만 근태 행을 생성한다.

```text
attendance_type = 결근
```

---

## 6. 결근 / 유급휴가 / 무급휴가

세 업무는 모두 관리자 확정 업무다.

공통 규칙:

```text
대상 직원
+
대상 날짜
↓
스케줄 조회
↓
스케줄 없음
→ 처리 불가
↓
schedule_day_type = 휴무일
→ 처리 불가
↓
기존 근태 확인
↓
이미 근태 있음
→ 신규 생성 불가
↓
해당 날짜 적용 계약 조회
↓
근태 생성
```

결근:

```text
attendance_type = 결근
work_day_type = 근무일
clock_in = null
clock_out = null
break_minutes = 0
night_hours = 0
overtime_hours = 0
```

유급휴가:

```text
attendance_type = 유급휴가
work_day_type = 근무일
clock_in = null
clock_out = null
break_minutes = 0
night_hours = 0
overtime_hours = 0
```

무급휴가:

```text
attendance_type = 무급휴가
work_day_type = 근무일
clock_in = null
clock_out = null
break_minutes = 0
night_hours = 0
overtime_hours = 0
```

### 미래 근태

업무 규칙은 다음으로 확정한다.

```text
과거 attendance 생성 가능
오늘 attendance 생성 가능
미래 attendance 생성 불가
```

미래는 실제 근태가 아니라 스케줄 영역이다.

따라서 관리자가 미래 날짜에 결근/유급휴가/무급휴가를 미리 생성하는 것은 금지한다.

※ 실제 Service 검증 코드는 후속 패치 항목으로 남겨둔다.

---

## 7. 휴무일

```text
schedule_day_type = 휴무일
attendance 없음
```

은 정상적인 휴무다.

휴무일마다 의미 없는 attendance 행을 만들지 않는다.

---

## 8. 근태 정정

### 정상 → 비근무

```text
정상
→ 결근 / 유급휴가 / 무급휴가
```

변경:

```text
clock_in = null
clock_out = null
break_minutes = 0
night_hours = 0
overtime_hours = 0
attendance_type = 변경된 유형
```

유지:

```text
emp_attendance_no
work_schedule_no
contract_no
work_date
work_day_type
```

### 비근무 → 정상

실제 근무정보를 다시 입력하고 계산한다.

```text
clock_in
clock_out
break_minutes
↓
실제근무시간 계산
↓
overtime_hours 계산
↓
night_hours 계산
↓
attendance_type = 정상
```

### 비근무 ↔ 비근무

```text
결근 ↔ 유급휴가 ↔ 무급휴가
```

이면 실질적으로 `attendance_type`만 변경한다.

---

## 9. 자기 자신의 근태 정정 금지

최종 규칙:

```text
본인 근태 조회
→ 가능

본인 출근/퇴근
→ 가능

본인 근태 정정
→ 불가

타 직원 근태 정정
→ 관리자 권한이 있으면 가능
```

권한 실제 연결은 후속 패치에서 기존 권한/계약 보조 Service를 재사용한다.

---

## 10. 상태 조합

`attendanceStatus`라는 별도 필드는 만들지 않는다.

현재 존재하는 사실만 조합한다.

| schedule | attendance | 의미 |
|---|---|---|
| 근무일 | 없음 | 미출근 |
| 근무일 | 정상 | 정상 근무 |
| 근무일 | 결근 | 결근 |
| 근무일 | 유급휴가 | 유급휴가 |
| 근무일 | 무급휴가 | 무급휴가 |
| 휴무일 | 없음 | 정상 휴무 |
| 휴무일 | 정상 | 휴무일 근무 |
| 휴무일 | 결근 | 불가 |
| 휴무일 | 유급휴가 | 불가 |
| 휴무일 | 무급휴가 | 불가 |

---

# 2단계. 테이블 역할 검증

## 1. employee_work_schedule

```text
work_schedule_no
employee_no
schedule_date
schedule_day_type
```

역할:

```text
예정
```

제약:

```text
PK(work_schedule_no)
UNIQUE(employee_no, schedule_date)
CHECK(schedule_day_type IN ('근무일', '휴무일'))
```

스케줄은 최종적으로:

```text
등록 O
정정 O
삭제 X
```

이다.

### 스케줄 정정 최종 규칙

아직 해당 스케줄을 참조하는 attendance가 없다면:

```text
employeeNo 수정 가능
scheduleDate 수정 가능
scheduleDayType 수정 가능
```

attendance가 이미 존재하면:

```text
employeeNo 수정 불가
scheduleDate 수정 불가
scheduleDayType 수정 불가
```

즉 실제 결과가 발생한 뒤 예정 이력을 바꾸지 않는다.

---

## 2. employee_attendance

최종 컬럼:

```text
emp_attendance_no
contract_no
work_schedule_no
work_date
clock_in
clock_out
break_minutes
attendance_type
work_day_type
night_hours
overtime_hours
```

`break_start`, `break_end`는 없다.

`attendanceStatus`도 DB 컬럼이 아니며 현재 설계에서는 사용하지 않는다.

제약:

```text
PK(emp_attendance_no)
FK(contract_no → employment_contract.contract_no)
FK(work_schedule_no → employee_work_schedule.work_schedule_no)
UNIQUE(work_schedule_no)

CHECK(attendance_type IN ('정상','결근','유급휴가','무급휴가'))
CHECK(work_day_type IN ('근무일','휴무일'))
```

---

## 3. 계약 휴게시간과 근태 휴게시간 이름 분리

계약 테이블의 휴게시간은 계약서에 작성된 값이라는 의미가 드러나도록 이름을 분리한다.

```text
employment_contract.written_break_minutes
= 계약서에 작성된 휴게시간(분)

employee_attendance.break_minutes
= 해당 근태에 적용된 휴게시간(분)
```

Java:

```java
ContractDto.writtenBreakMinutes
EmployeeAttendanceDto.breakMinutes
```

현재 별도 휴게 시작/종료 이벤트를 저장하지 않으므로 출근 생성 시 계약의 `writtenBreakMinutes`를 해당 근태의 기본 `breakMinutes`로 적용하고, 관리자가 근무시간을 정정할 때 근태의 `breakMinutes`를 정정할 수 있다.

비근무 근태는 항상:

```text
break_minutes = 0
```

이다.

계약 모듈에서는 컬럼명 변경을 다음 위치에 전파한다.

```text
ContractDto
ContractAddRequestVO
ContractUpdateRequestVO
관련 Contract ResponseVO
contractAdd Mapper
updateDraft Mapper
update Mapper
관련 SELECT Mapper
```

---

## 4. 계약 적용기간 경계

근로조건 변경 시 기존 계약 종료시각과 신규 계약 시작시각을 동일하게 둔다.

```text
기존 계약
[start, newStart)

신규 계약
[newStart, ...)
```

즉 최종 의미는:

```text
contractStart 포함
contractEnd 미포함
```

이다.

```text
contractStart <= 기준시각 < contractEnd
```

근로조건 변경 예:

```java
originDto.setContractEnd(newContractDto.getContractStart());
originDto.setContractStatus("ended");
```

`findApplicable()`도 반드시 같은 경계 규칙을 사용한다.

---

# 3단계. 필요한 기능 목록

## A. 근무 스케줄

```text
- 직원 근무 스케줄 등록
- 직원 근무 스케줄 정정
- 특정 직원 특정일 스케줄 조회
- 특정 직원 기간별 스케줄 조회
- 특정 날짜 전체 직원 스케줄 조회
- 본인 오늘 스케줄 조회
- 본인 특정일 스케줄 조회
- 본인 기간별 스케줄 조회
```

삭제 기능은 없다.

---

## B. 실제 출퇴근

```text
- 출근
- 퇴근
```

출근 시 스케줄은 필수다.

```text
스케줄 없음 → 출근 불가
근무일 → 정상 출근
휴무일 → 휴무일 근무로 출근 가능
```

퇴근은 현재 열린 근태를 기준으로 한다.

---

## C. 비근무 근태 확정

```text
- 결근 기록
- 유급휴가 기록
- 무급휴가 기록
```

---

## D. 근태 정정

```text
- 정상 근태의 출근/퇴근/휴게분 정정
- 정상 → 비근무
- 비근무 → 정상
- 비근무 상태끼리 유형 변경
```

---

## E. 근태 조회

```text
- 본인 오늘 근태
- 본인 기간별 근태
- 관리자 특정 직원 기간별 근태
- 관리자 특정 날짜 전체 근태
- 근태 상세 조회
```

미출근/휴무도 조회 대상이 될 수 있으므로 목록성 조회는 스케줄을 기준으로 attendance를 LEFT JOIN한다.

---

## F. 공통 업무 판단

```text
- 스케줄 존재 확인
- 근태 중복 확인
- 열린 근태 확인
- 자기 근태 정정 금지
- 적용 계약 확인
- 계약기간 확인
- 근태유형 정합성 확인
- 미래 attendance 생성 금지
```

---

## G. 시간 계산

```text
- 실제 근무시간 계산
- 연장근로시간 계산
- 야간근로시간 계산
```

시간 계산은 내부에서 분 단위 정수로 계산하고 최종 저장 시 시간 단위 `double`로 변환한다.

```java
double hours = minutes / 60.0;
```

`0.5`시간 같은 값을 보존해야 하므로 DTO의 `nightHours`, `overtimeHours`는 `double`을 유지한다.

---

# 4단계. RequestVO / ResponseVO

## 1. RequestVO 원칙

RequestVO에는 외부 사용자가 실제로 결정하는 값만 받는다.

Service가 찾아야 하는 값:

```text
contractNo
workScheduleNo
기존 attendance
```

Service가 계산해야 하는 값:

```text
nightHours
overtimeHours
```

이런 값을 출근 Request에서 프론트에게 받지 않는다.

---

## 2. 스케줄 RequestVO

### EmployeeWorkScheduleAddRequestVO

```java
private long employeeNo;
private LocalDateTime scheduleDate;
private String scheduleDayType;
```

### EmployeeWorkScheduleUpdateRequestVO

최종 수정 범위가 세 필드 모두이므로:

```java
private long workScheduleNo;
private long employeeNo;
private LocalDateTime scheduleDate;
private String scheduleDayType;
```

### EmployeeWorkScheduleSearchRequestVO

```java
private long employeeNo;
private LocalDateTime startDate;
private LocalDateTime endDate;
```

삭제 RequestVO는 없고 삭제 API 자체도 없다.

---

## 3. 출근 / 퇴근 RequestVO

본인 출근과 퇴근은 RequestVO가 필요 없다.

```text
직원
→ 인증정보

시간
→ 서버시간

스케줄
→ Service 조회

계약
→ Service 조회
```

---

## 4. 결근 / 휴가 RequestVO

### AttendanceAbsentRequestVO

```java
private long employeeNo;
private LocalDateTime workDate;
```

### AttendanceLeaveRequestVO

```java
private long employeeNo;
private LocalDateTime workDate;
```

유급/무급은 Service 메서드 자체로 업무를 구분한다.

---

## 5. 근태 정정 RequestVO

### AttendanceWorkTimeUpdateRequestVO

```java
private long empAttendanceNo;
private LocalDateTime clockIn;
private LocalDateTime clockOut;
private Integer breakMinutes;
```

### AttendanceTypeUpdateRequestVO

```java
private long empAttendanceNo;
private String attendanceType;
```

### AttendanceRestoreWorkRequestVO

```java
private long empAttendanceNo;
private LocalDateTime clockIn;
private LocalDateTime clockOut;
private Integer breakMinutes;
```

`nightHours`, `overtimeHours`는 Request에서 받지 않고 Service가 다시 계산한다.

---

## 6. 조회 RequestVO

### AttendancePeriodRequestVO

```java
private LocalDateTime startDate;
private LocalDateTime endDate;
```

### EmployeeAttendanceSearchRequestVO

```java
private long employeeNo;
private LocalDateTime startDate;
private LocalDateTime endDate;
```

---

## 7. ResponseVO

`attendanceStatus`는 사용하지 않는다.

화면은 스케줄 사실과 근태 사실을 전달받아 표현한다.

### EmployeeWorkScheduleResponseVO

```java
private long workScheduleNo;
private long employeeNo;
private LocalDateTime scheduleDate;
private String scheduleDayType;
```

### AttendanceTodayResponseVO

```java
private Long empAttendanceNo;
private Long workScheduleNo;
private LocalDateTime workDate;
private String workDayType;
private String attendanceType;
private LocalDateTime clockIn;
private LocalDateTime clockOut;
private Integer breakMinutes;
private Double nightHours;
private Double overtimeHours;
```

근태가 아직 없으면 attendance 관련 필드는 null일 수 있다.

### AttendanceListResponseVO

```java
private Long empAttendanceNo;
private long workScheduleNo;
private LocalDateTime workDate;
private String attendanceType;
private String workDayType;
private LocalDateTime clockIn;
private LocalDateTime clockOut;
```

### EmployeeAttendanceListResponseVO

```java
private long employeeNo;
private Long empAttendanceNo;
private long workScheduleNo;
private LocalDateTime workDate;
private String attendanceType;
private String workDayType;
private LocalDateTime clockIn;
private LocalDateTime clockOut;
```

### AttendanceDetailResponseVO

```java
private long empAttendanceNo;
private long workScheduleNo;
private long contractNo;
private long employeeNo;
private LocalDateTime workDate;
private LocalDateTime clockIn;
private LocalDateTime clockOut;
private int breakMinutes;
private String attendanceType;
private String workDayType;
private double nightHours;
private double overtimeHours;
```

`employeeName`은 정확한 직원 정보 조회원이 확정되기 전까지 억지로 넣지 않는다.

---

# 5단계. DTO

## EmployeeWorkScheduleDto

```java
@Data
public class EmployeeWorkScheduleDto {

    private long workScheduleNo;
    private long employeeNo;
    private LocalDateTime scheduleDate;
    private String scheduleDayType;
}
```

DB 컬럼과 1:1이다.

---

## EmployeeAttendanceDto

```java
@Data
public class EmployeeAttendanceDto {

    private long empAttendanceNo;
    private long workScheduleNo;
    private long contractNo;

    private LocalDateTime workDate;
    private LocalDateTime clockIn;
    private LocalDateTime clockOut;

    private int breakMinutes;

    private String attendanceType;
    private String workDayType;

    private double nightHours;
    private double overtimeHours;
}
```

DTO에는 다음을 넣지 않는다.

```text
employeeName X
attendanceStatus X
employeeNo X   // employee_attendance 테이블 컬럼이 아님
breakStart X
breakEnd X
```

직원 번호는 스케줄 또는 계약을 통해 확인한다.

---

# 6단계. DAO 명세

## 1. EmployeeWorkScheduleDao

```java
public interface EmployeeWorkScheduleDao {

    long sequence();

    void add(EmployeeWorkScheduleDto dto);

    EmployeeWorkScheduleDto find(long workScheduleNo);

    EmployeeWorkScheduleDto findByEmployeeAndDate(
            long employeeNo,
            LocalDateTime scheduleDate);

    List<EmployeeWorkScheduleDto> findByEmployeeAndPeriod(
            long employeeNo,
            LocalDateTime startDate,
            LocalDateTime endDate);

    List<EmployeeWorkScheduleDto> findByDate(
            LocalDateTime scheduleDate);

    boolean update(EmployeeWorkScheduleDto dto);
}
```

`delete()`는 없다.

---

## 2. EmployeeAttendanceDao

```java
public interface EmployeeAttendanceDao {

    long sequence();

    void add(EmployeeAttendanceDto dto);

    EmployeeAttendanceDto find(long empAttendanceNo);

    EmployeeAttendanceDto findBySchedule(long workScheduleNo);

    EmployeeAttendanceDto findWorkingByEmployee(long employeeNo);

    boolean clockOut(EmployeeAttendanceDto dto);

    boolean updateWorkTime(EmployeeAttendanceDto dto);

    boolean changeToNonWork(EmployeeAttendanceDto dto);

    boolean changeToWork(EmployeeAttendanceDto dto);

    boolean changeType(EmployeeAttendanceDto dto);

    List<AttendanceScheduleConnectVO> findScheduleAndAttendanceByEmployeeAndPeriod(
            long employeeNo,
            LocalDateTime startDate,
            LocalDateTime endDate);

    List<AttendanceScheduleConnectVO> findScheduleAndAttendanceByDate(
            LocalDateTime workDate);
}
```

---

## 3. AttendanceScheduleConnectVO

스케줄을 기준으로 LEFT JOIN하면 attendance가 없을 수 있으므로 근태 필드는 wrapper를 사용한다.

```java
@Data
public class AttendanceScheduleConnectVO {

    private long workScheduleNo;
    private long employeeNo;
    private LocalDateTime scheduleDate;
    private String scheduleDayType;

    private Long empAttendanceNo;
    private Long contractNo;
    private LocalDateTime workDate;
    private LocalDateTime clockIn;
    private LocalDateTime clockOut;
    private Integer breakMinutes;
    private String attendanceType;
    private String workDayType;
    private Double nightHours;
    private Double overtimeHours;
}
```

---

## 4. ContractDao 추가 행동

근태 Service가 날짜 기준 적용 계약을 찾아야 하므로:

```java
ContractDto findApplicable(long employeeNo, LocalDateTime workDate);
```

개념이 필요하다.

퇴근/정정 계산에는 기존:

```java
findWorkTimeCondition(contractNo)
```

을 이용한다.

`findApplicable()`은 상태를 단순히 `active`로 제한하면 안 된다.

과거 근태/휴가 처리에서는 이미 `ended`가 된 과거 계약도 해당 기간에 적용됐던 계약으로 조회되어야 하기 때문이다.

따라서 핵심 기준은:

```text
employee_no
signed_time is not null
contract_start <= workDate
contract_end is null OR workDate < contract_end
```

이다.

---

# 7단계. Mapper

namespace는 기존 스타일대로 `mapper.payroll`을 유지하고 statement id는 충돌 방지를 위해 접두어를 사용한다.

---

## 1. employee_work_schedule

### sequence

```xml
<select id="workScheduleSequence" resultType="long">
    select employee_work_schedule_seq.nextval from dual
</select>
```

### add

```xml
<insert id="workScheduleAdd">
    insert into employee_work_schedule (
        work_schedule_no,
        employee_no,
        schedule_date,
        schedule_day_type
    )
    values (
        #{workScheduleNo},
        #{employeeNo},
        #{scheduleDate},
        #{scheduleDayType}
    )
</insert>
```

### find

```xml
<select id="workScheduleFind" resultType="EmployeeWorkScheduleDto">
    select *
    from employee_work_schedule
    where work_schedule_no = #{workScheduleNo}
</select>
```

### employee + date

```xml
<select id="workScheduleFindByEmployeeAndDate"
        resultType="EmployeeWorkScheduleDto">
    select *
    from employee_work_schedule
    where employee_no = #{employeeNo}
        and schedule_date = #{scheduleDate}
</select>
```

### period

```xml
<select id="workScheduleFindByEmployeeAndPeriod"
        resultType="EmployeeWorkScheduleDto">
    select *
    from employee_work_schedule
    where employee_no = #{employeeNo}
        and schedule_date <![CDATA[ >= ]]> #{startDate}
        and schedule_date <![CDATA[ < ]]> #{endDate}
    order by schedule_date asc
</select>
```

### update 최종본

```xml
<update id="workScheduleUpdate">
    update employee_work_schedule
    set employee_no = #{employeeNo},
        schedule_date = #{scheduleDate},
        schedule_day_type = #{scheduleDayType}
    where work_schedule_no = #{workScheduleNo}
</update>
```

스케줄 DELETE Mapper는 없다.

---

## 2. employee_attendance 핵심 Mapper

### add

```xml
<insert id="attendanceAdd">
    insert into employee_attendance (
        emp_attendance_no,
        work_schedule_no,
        contract_no,
        work_date,
        clock_in,
        clock_out,
        break_minutes,
        attendance_type,
        work_day_type,
        night_hours,
        overtime_hours
    )
    values (
        #{empAttendanceNo},
        #{workScheduleNo},
        #{contractNo},
        #{workDate},
        #{clockIn},
        #{clockOut},
        #{breakMinutes},
        #{attendanceType},
        #{workDayType},
        #{nightHours},
        #{overtimeHours}
    )
</insert>
```

### schedule 기준 조회

```xml
<select id="attendanceFindBySchedule"
        resultType="EmployeeAttendanceDto">
    select *
    from employee_attendance
    where work_schedule_no = #{workScheduleNo}
</select>
```

### 직원의 현재 열린 근태 조회

```xml
<select id="attendanceFindWorkingByEmployee"
        resultType="EmployeeAttendanceDto">
    select A.*
    from employee_attendance A
        inner join employee_work_schedule S
            on A.work_schedule_no = S.work_schedule_no
    where S.employee_no = #{employeeNo}
        and A.attendance_type = '정상'
        and A.clock_in is not null
        and A.clock_out is null
</select>
```

이 조회는 야간근무 때문에 `오늘 날짜` 조건을 넣지 않는다.

### 퇴근

```xml
<update id="attendanceClockOut">
    update employee_attendance
    set clock_out = #{clockOut},
        night_hours = #{nightHours},
        overtime_hours = #{overtimeHours}
    where emp_attendance_no = #{empAttendanceNo}
</update>
```

### 정상 근태 시간 정정

```xml
<update id="attendanceUpdateWorkTime">
    update employee_attendance
    set clock_in = #{clockIn},
        clock_out = #{clockOut},
        break_minutes = #{breakMinutes},
        night_hours = #{nightHours},
        overtime_hours = #{overtimeHours}
    where emp_attendance_no = #{empAttendanceNo}
</update>
```

### 정상 → 비근무

```xml
<update id="attendanceChangeToNonWork">
    update employee_attendance
    set attendance_type = #{attendanceType},
        clock_in = null,
        clock_out = null,
        break_minutes = 0,
        night_hours = 0,
        overtime_hours = 0
    where emp_attendance_no = #{empAttendanceNo}
</update>
```

### 비근무 → 정상

```xml
<update id="attendanceChangeToWork">
    update employee_attendance
    set attendance_type = '정상',
        clock_in = #{clockIn},
        clock_out = #{clockOut},
        break_minutes = #{breakMinutes},
        night_hours = #{nightHours},
        overtime_hours = #{overtimeHours}
    where emp_attendance_no = #{empAttendanceNo}
</update>
```

### 비근무 유형 변경

```xml
<update id="attendanceChangeType">
    update employee_attendance
    set attendance_type = #{attendanceType}
    where emp_attendance_no = #{empAttendanceNo}
</update>
```

### 스케줄 + 근태 기간조회

```xml
<select id="attendanceFindScheduleAndAttendanceByEmployeeAndPeriod"
        resultType="AttendanceScheduleConnectVO">
    select
        S.work_schedule_no,
        S.employee_no,
        S.schedule_date,
        S.schedule_day_type,
        A.emp_attendance_no,
        A.contract_no,
        A.work_date,
        A.clock_in,
        A.clock_out,
        A.break_minutes,
        A.attendance_type,
        A.work_day_type,
        A.night_hours,
        A.overtime_hours
    from employee_work_schedule S
        left outer join employee_attendance A
            on S.work_schedule_no = A.work_schedule_no
    where S.employee_no = #{employeeNo}
        and S.schedule_date <![CDATA[ >= ]]> #{startDate}
        and S.schedule_date <![CDATA[ < ]]> #{endDate}
    order by S.schedule_date asc
</select>
```

---

## 3. Contract findApplicable

```xml
<select id="findApplicableContract"
        resultType="ContractDto">
    select *
    from employment_contract
    where employee_no = #{employeeNo}
        and signed_time is not null
        and contract_start <![CDATA[ <= ]]> #{workDate}
        and (
            contract_end is null
            or contract_end <![CDATA[ > ]]> #{workDate}
        )
    order by contract_start desc
</select>
```

핵심:

```text
contract_start <= workDate < contract_end
```

`>= contract_end` 방식은 근로조건 변경 경계에서 기존/신규 계약을 동시에 잡을 수 있으므로 사용하지 않는다.

---

# 8단계. Service 명세

## 1. EmployeeWorkScheduleService

```java
public interface EmployeeWorkScheduleService {

    void add(EmployeeWorkScheduleAddRequestVO requestVO);

    void update(EmployeeWorkScheduleUpdateRequestVO requestVO);

    EmployeeWorkScheduleResponseVO findByEmployeeAndDate(
            long employeeNo,
            LocalDateTime scheduleDate);

    List<EmployeeWorkScheduleResponseVO> findByEmployeeAndPeriod(
            EmployeeWorkScheduleSearchRequestVO requestVO);

    List<EmployeeWorkScheduleResponseVO> findByDate(
            LocalDateTime scheduleDate);

    EmployeeWorkScheduleResponseVO findMyToday(
            TokenParseResponseVO parseVO);

    EmployeeWorkScheduleResponseVO findMyDate(
            LocalDateTime scheduleDate,
            TokenParseResponseVO parseVO);

    List<EmployeeWorkScheduleResponseVO> findMyPeriod(
            AttendancePeriodRequestVO requestVO,
            TokenParseResponseVO parseVO);
}
```

삭제 메서드는 없다.

---

## 2. 스케줄 등록

```text
scheduleDayType 검증
↓
scheduleDate 00:00 정규화
↓
같은 employeeNo + scheduleDate 중복 확인
↓
중복이면 거부
↓
sequence 발급
↓
DTO 조립
↓
등록
```

---

## 3. 스케줄 수정 최종 규칙

```text
request 값 검증
↓
scheduleDate 00:00 정규화
↓
workScheduleNo로 현재 스케줄 조회
↓
없음 → TargetNotfoundException
↓
attendanceDao.findBySchedule(workScheduleNo)
↓
attendance 존재 → 정정 불가
↓
새 employeeNo + 새 scheduleDate 중복 스케줄 확인
↓
다른 workScheduleNo가 이미 존재 → 거부
↓
변경점 없으면 종료
↓
employeeNo / scheduleDate / scheduleDayType 전부 UPDATE
```

---

## 4. EmployeeAttendanceService 기능

```text
[직원]
clockIn
clockOut
findMyToday
findMyPeriod

[관리자 비근무]
recordAbsence
recordPaidLeave
recordUnpaidLeave

[관리자 정정]
updateWorkTime
changeToNonWork
changeToWork
changeNonWorkType

[관리자 조회]
findByEmployeeAndPeriod
findByDate
findDetail
```

---

## 5. 시간 계산 규칙

DB/DTO는:

```java
double nightHours;
double overtimeHours;
```

유지.

하지만 Service 내부 계산은 분 단위 정수로 한다.

```text
clockIn / clockOut
↓
Duration → minutes
↓
breakMinutes 차감
↓
actualWorkMinutes
↓
연장/야간 계산
↓
마지막에 / 60.0
```

이유:

```text
0.5시간 같은 소수시간은 필요
하지만 계산 중간부터 double 연산을 반복할 이유는 없음
```

---

# 9단계. ServiceImpl 핵심 흐름

## 1. 스케줄 update

핵심 구현 순서:

```java
validateScheduleDayType(requestVO.getScheduleDayType());

LocalDateTime scheduleDate = requestVO.getScheduleDate()
        .toLocalDate()
        .atStartOfDay();

EmployeeWorkScheduleDto current =
        workScheduleDao.find(requestVO.getWorkScheduleNo());

if (current == null) {
    throw new TargetNotfoundException();
}

EmployeeAttendanceDto attendance =
        attendanceDao.findBySchedule(current.getWorkScheduleNo());

if (attendance != null) {
    throw new GetOutException();
}

EmployeeWorkScheduleDto duplicate =
        workScheduleDao.findByEmployeeAndDate(
                requestVO.getEmployeeNo(),
                scheduleDate);

if (duplicate != null
        && duplicate.getWorkScheduleNo() != current.getWorkScheduleNo()) {
    throw new GetOutException();
}

current.setEmployeeNo(requestVO.getEmployeeNo());
current.setScheduleDate(scheduleDate);
current.setScheduleDayType(requestVO.getScheduleDayType());

workScheduleDao.update(current);
```

실제 예외 메시지는 프로젝트 스타일에 맞춘다.

---

## 2. clockIn

```text
로그인 직원 식별
↓
오늘 날짜 00:00 확정
↓
오늘 스케줄 조회
↓
없음 → 출근 불가
↓
현재 열린 근태 존재 확인
↓
있음 → 중복 출근 불가
↓
findApplicable(employeeNo, workDate)
↓
적용 계약 없음 → 출근 불가
↓
해당 schedule의 attendance 존재 확인
↓
있음 → 출근 불가
↓
근태 생성
```

생성값:

```text
emp_attendance_no = sequence
work_schedule_no = 오늘 스케줄
contract_no = 해당 날짜 적용 계약
work_date = 출근 시작 날짜
clock_in = 서버 현재시각
clock_out = null
break_minutes = contract.written_break_minutes
attendance_type = 정상
work_day_type = schedule.schedule_day_type
night_hours = 0
overtime_hours = 0
```

---

## 3. clockOut

오늘 스케줄이 아니라 **현재 열린 근태**를 찾는다.

```text
employeeNo
↓
findWorkingByEmployee(employeeNo)
↓
없음 → 퇴근 불가
↓
clockOut = 서버 현재시각
↓
clockOut > clockIn 검증
↓
breakMinutes 검증
↓
attendance.contractNo로 계약 근로시간 조회
↓
실제근무시간 계산
↓
overtimeHours 계산
↓
nightHours 계산
↓
저장
```

`work_date`는 출근을 시작한 날짜를 유지한다.

---

## 4. 결근 / 휴가

공통 흐름:

```text
대상 employeeNo + workDate
↓
미래 날짜 여부 검증
↓
해당 날짜 스케줄 조회
↓
근무일 확인
↓
기존 attendance 확인
↓
해당 날짜 적용 계약 확인
↓
비근무 attendance 생성
```

미래 attendance 금지는 확정 규칙이지만 실제 `validateNotFuture()` 구현은 후속 패치에서 진행한다.

---

## 5. 관리자 정정

### updateWorkTime

```text
attendance 조회
↓
대상 employeeNo 확인
↓
자기 근태 수정 금지
↓
attendance_type = 정상 확인
↓
clockIn / clockOut / breakMinutes 검증
↓
workDate와 clockIn 시작날짜 정합성 확인
↓
contractNo 기준 계약 근로시간 조회
↓
연장/야간 재계산
↓
UPDATE
```

### changeToNonWork

```text
현재 정상인지 확인
work_day_type = 근무일 확인
자기 근태 수정 금지
↓
clockIn/out 제거
breakMinutes = 0
night/overtime = 0
attendanceType 변경
```

### changeToWork

```text
현재 비근무인지 확인
work_day_type = 근무일 확인
자기 근태 수정 금지
↓
실제 clockIn / clockOut / breakMinutes 입력 검증
↓
계약조건 조회
↓
연장/야간 계산
↓
attendanceType = 정상
```

### changeNonWorkType

```text
현재 비근무인지 확인
요청 유형도 비근무인지 확인
자기 근태 수정 금지
↓
attendance_type만 UPDATE
```

---

## 6. 기간 조회

근태만 조회하지 않는다.

```text
employee_work_schedule
LEFT JOIN employee_attendance
```

를 사용한다.

그래야:

```text
근무일 + attendance 없음
휴무일 + attendance 없음
```

도 조회 결과에서 사라지지 않는다.

기간 입력은:

```text
startDate = 시작일 00:00
endDate = 종료일 다음날 00:00
```

로 바꾸고 SQL은:

```text
>= startDate
< endDate
```

로 통일한다.

---

# 10단계. Controller

Controller는 HTTP만 담당한다.

```text
HTTP 요청 받기
↓
인증정보 받기
↓
RequestVO 받기
↓
Service 호출
↓
Response 반환
```

다음은 Controller에서 하지 않는다.

```text
DAO 직접 호출 X
DTO 직접 조립 X
업무 검증 X
시간 계산 X
권한 규칙 직접 구현 X
```

---

## 1. 기본 경로

최종 base path:

```text
/api/work-schedule
/api/attendance
```

예:

```java
@RestController
@RequestMapping("/api/work-schedule")
@CommonsApiResponse
public class EmployeeWorkScheduleController {
}
```

```java
@RestController
@RequestMapping("/api/attendance")
@CommonsApiResponse
public class EmployeeAttendanceController {
}
```

---

## 2. @Valid

RequestBody를 받는 API는:

```java
@Valid @RequestBody ...
```

형태로 정리한다.

예:

```java
@PostMapping
public void add(
        @Valid @RequestBody EmployeeWorkScheduleAddRequestVO requestVO) {

    workScheduleService.add(requestVO);
}
```

RequestVO에는 입력 형태 검증만 둔다.

예:

```java
@Positive
private long employeeNo;

@NotNull
private LocalDateTime scheduleDate;

@NotBlank
private String scheduleDayType;
```

`근무일/휴무일 중 하나인가` 같은 업무 검증은 Service에서 한다.

---

## 3. 인증 사용자

현재 `TokenParseResponseVO`:

```java
public class TokenParseResponseVO {
    private int accountNo;
    private String accountId;
    private String accountType;
    private List<String> roleNames;
    private int noType;
}
```

직원 계정이면:

```text
noType = employeeNo
```

이다.

본인 API는 Controller에서:

```java
@CurrentUser TokenParseResponseVO parseVO
```

를 받아 Service로 전달하는 형태로 정리한다.

예:

```java
@PostMapping("/clock-in")
public void clockIn(
        @CurrentUser TokenParseResponseVO parseVO) {

    attendanceService.clockIn(parseVO);
}
```

```java
@PostMapping("/clock-out")
public void clockOut(
        @CurrentUser TokenParseResponseVO parseVO) {

    attendanceService.clockOut(parseVO);
}
```

단, `noType → employeeNo` 실제 연결과 관리자/본인 권한 검증은 후속 패치 2/3에서 기존 권한/계약 보조 Service를 재사용해 연결한다.

Controller에서 임시로 `roleNames.contains(...)` 같은 업무권한 로직을 박지 않는다.

---

## 4. 스케줄 삭제 API 없음

최종 규칙:

```text
등록 O
정정 O
삭제 X
```

따라서:

```text
@DeleteMapping X
Service.delete X
DAO.delete X
Mapper <delete> X
```

전부 없다.

---

# 최종 패치 상태

현재 설계 문서 기준:

```text
[반영 완료]
1. 스케줄 update 최종설계
   - attendance 전 employeeNo / scheduleDate / scheduleDayType 수정 가능
   - attendance 후 전체 수정 불가
   - 삭제 기능 제거

4. Contract findApplicable 기간 경계
   - [start, end)
   - contract_start <= workDate < contract_end

7. 계약 휴게시간 명칭 분리
   - written_break_minutes
   - writtenBreakMinutes
   - attendance.breakMinutes와 의미 구분

8. 시간값 자료형
   - nightHours / overtimeHours = double
   - 내부 계산은 분 단위 정수 후 / 60.0

9. Controller 정리
   - /api/work-schedule
   - /api/attendance
   - @CommonsApiResponse
   - @CurrentUser
   - @Valid
   - DELETE 없음

[규칙 확정, 실제 구현 후속]
5. 미래 attendance 생성 금지

[후속 패치]
2. 관리자 / 본인 API 권한 실제 연결
3. TokenParseResponseVO.noType → employeeNo 연결
```

이 문서에는 현재 DB/DTO에 없는 `attendanceStatus`, `breakStart`, `breakEnd`, `휴게중` 개념을 사용하지 않는다.
