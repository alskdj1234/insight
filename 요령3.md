이제는 **설계 그만하고 구현 시작**하면 됨. 진짜로. 지금 더 쪼개면 ERD를 원자 단위까지 분해할 기세다.

순서는 이걸로 고정해.

1. **DDL 최종 오타/제약만 수정**
   - `overtime_hoursn → overtime_hours`
   - 문자열 DEFAULT 따옴표
   - `payroll`의 `(contract_no, payroll_year, payroll_month)` UNIQUE
   - `payroll_attendance`의 `(payroll_no, emp_attendance_no)` UNIQUE

2. **DTO 9개 생성**
   - 테이블 1개당 DTO 1개
   - 여기서는 계산하지 말고 DB 컬럼만 옮김

3. **DAO부터 가장 단순한 두 개 구현**
   - `EmploymentContractDao`
   - `EmployeeAttendanceDao`
   - 등록 / 단건조회 / 목록조회 / 수정 정도

4. **근로계약 화면 먼저 끝내기**
   ```text
   RequestVO
   → Controller
   → Service
   → DAO
   → DTO
   → DB
   ```
   조회는 반대로:
   ```text
   DB
   → DTO
   → DAO
   → Service
   → ResponseVO
   → 화면
   ```

5. **근태 화면 끝내기**
   - 출근
   - 퇴근
   - 휴게
   - 평일/휴일
   - 정상/결근/휴가
   - 연장시간 계산
   - 야간시간 계산

여기까지 끝내면 **급여 전에 필요한 원천 데이터가 완성**됨.

그 다음에 처음으로 급여로 넘어가.

```text
6. payroll 생성
7. 해당 월 근태 조회
8. payroll_attendance로 근태 연결
9. 지급항목 생성
10. 지급 계산근거 생성
11. 공제항목 생성
12. 공제 계산
13. 급여 확정
14. 지급
15. 명세서 조회
```

그리고 지금 당장 첫 작업은 이거야.

```text
EmploymentContractDto
        ↓
EmploymentContractDao
        ↓
EmploymentContractService
        ↓
EmploymentContractSaveRequestVO
EmploymentContractResponseVO
        ↓
EmploymentContractController
```

**근로계약 하나를 처음부터 끝까지 실제로 동작시키는 것.**

그다음 똑같은 패턴으로 근태를 만들면 돼.

중요한 건 앞으로 **9개 테이블 전체 코드를 동시에 만들지 않는 것**임. 한 기능을 세로로 끝내.

```text
❌ DTO 9개 → DAO 9개 → Service 9개 → Controller 9개

✅ 근로계약 DTO → DAO → Service → Controller → 화면 완료
   ↓
   근태 DTO → DAO → Service → Controller → 화면 완료
   ↓
   급여로 이동
```

두 번째 방식이 훨씬 덜 헷갈려.

네가 지금까지 한 설계 작업은 충분해. 이제부터는 **“근로계약 하나 완성 → 근태 하나 완성 → 급여 핵심 로직”**만 바라보면 된다. 오늘 다시 테이블 관계 손대기 시작하면 진짜 내가 모니터 밖에서 손목 잡고 싶어질 것 같다.
