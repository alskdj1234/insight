맞아. 이제는 **DTO에서 서로 붙어 있는 데이터들을 묶어서 “DB에서 무슨 일을 할 건지”로 올리면 DAO가 됨.**

즉 이런 식임.

```text
DTO
(어느 날짜) 근무 → 근무날짜
(평일/휴일) 근무 → 근무일구분
출근 → 출근시각
퇴근 → 퇴근시각

        ↓ 인접한 것끼리 묶기

DAO
특정 날짜의 출퇴근 근태를 등록한다
특정 날짜의 근태를 조회한다
```

이 방식으로 9개 전부 가면 됨.

---

## 1. 근로계약 DAO

DTO에서:

```text
(직원) 계약 → 직원번호

(월급/시급/일급) 급여 → 급여형태
(계약상) 임금 → 기준임금

(1일) 근로 → 1일 근로시간
(1주) 근로 → 주 근로시간

(시작/종료) 계약 → 계약기간
(예정/진행중/종료) 계약 → 계약상태

(매월) 지급 → 지급예정일
```

인접한 것끼리 합치면:

```text
[근로계약 DAO]

직원의 근로계약을 등록한다

직원의 현재 근로계약을 조회한다

직원의 과거 근로계약을 조회한다

계약의 급여조건을 조회한다

계약의 근로시간 조건을 조회한다

계약기간과 계약상태를 조회한다

계약의 급여 지급예정일을 조회한다

근로계약 내용을 수정한다

근로계약의 상태와 종료일을 변경한다
```

핵심적으로는:

```text
직원 + 계약상태
→ 현재 계약 조회

직원 + 계약기간
→ 계약 이력 조회

급여형태 + 기준임금
→ 급여조건 조회

1일시간 + 주시간
→ 근로시간조건 조회
```

---

## 2. 직원근태 DAO

DTO:

```text
(어느 날) 근무 → 근무날짜
(평일/휴일) 근무 → 근무일구분

출근 → 출근시각
퇴근 → 퇴근시각
휴게 → 휴게시간

(연장) 근무 → 연장시간
(야간) 근무 → 야간시간

(정상/결근/휴가) 근태 → 근태구분
```

묶으면:

```text
[직원근태 DAO]

특정 날짜의 근태를 등록한다

특정 날짜의 근태를 조회한다

특정 계약의 월별 근태를 조회한다

출근·퇴근·휴게시간을 저장한다

연장·야간 근무시간을 저장한다

평일·휴일 근무구분을 저장한다

정상·결근·휴가 상태를 저장한다

기존 근태기록을 수정한다

잘못 등록된 근태기록을 삭제한다
```

여기가 특히 네가 말한 방식이 잘 보임.

```text
출근 + 퇴근 + 휴게
→ 출퇴근 근태 저장

연장 + 야간
→ 추가 근무시간 저장

평일/휴일 + 정상/결근/휴가
→ 근태 구분정보 저장
```

---

## 3. 급여반영근태 DAO

DTO:

```text
(급여) → 급여번호
(근태) → 근태번호
```

둘이 붙으면 그냥 DAO 역할이 바로 나옴.

```text
[급여반영근태 DAO]

급여에 근태를 반영한다

특정 급여에 반영된 근태를 조회한다

특정 근태가 어느 급여에 반영됐는지 조회한다

같은 근태가 이미 같은 급여에 반영됐는지 확인한다

급여와 근태의 연결을 해제한다
```

얘는 진짜:

```text
급여 + 근태
→ 급여에 반영된 근태
```

그 자체임.

---

## 4. 급여 DAO

DTO:

```text
(계약) 급여 → 계약번호

(연도) 급여 → 대상연도
(월) 급여 → 대상월

(산정중/확정/지급완료)
→ 급여상태
```

묶으면:

```text
[급여 DAO]

근로계약의 월 급여를 생성한다

특정 계약의 특정 연월 급여를 조회한다

직원의 월별 급여 목록을 조회한다

산정중인 급여를 조회한다

확정된 급여를 조회한다

지급완료된 급여를 조회한다

급여의 상태를 변경한다
```

여기서 핵심 조합:

```text
계약 + 연도 + 월
→ 월 급여 한 건 조회
```

이게 네 UNIQUE 걸었던 그 조합이기도 함.

---

## 5. 지급항목 DAO

DTO:

```text
(어떤 급여의) 지급
→ 급여번호

(기본급/수당/기타)
→ 지급종류

(기본급/주휴/연장/야간)
→ 지급항목명
```

묶으면:

```text
[지급항목 DAO]

급여의 지급항목을 등록한다

특정 급여의 모든 지급항목을 조회한다

특정 급여의 기본급을 조회한다

특정 급여의 수당들을 조회한다

특정 급여의 연장수당을 조회한다

특정 급여의 야간수당을 조회한다

지급항목을 수정한다

지급항목을 삭제한다
```

즉:

```text
급여 + 지급종류
→ 기본급/수당 목록 조회

급여 + 지급항목명
→ 특정 지급항목 조회
```

---

## 6. 지급 계산근거 DAO

DTO:

```text
(어떤 지급항목의) 계산
→ 지급항목번호

(기준) 금액
→ 기준금액/기준시급

(적용) 근무
→ 적용시간

(계산된) 지급
→ 최종 지급금액

(계산) 근거
→ 계산비고
```

묶으면:

```text
[지급 계산근거 DAO]

지급항목의 계산근거를 등록한다

특정 지급항목의 계산근거를 조회한다

기준금액과 적용시간을 저장한다

계산된 지급금액을 저장한다

지급금액의 계산근거를 저장한다

기존 계산근거를 수정한다
```

핵심은:

```text
기준금액 + 적용시간
→ 계산에 사용한 조건

지급금액 + 계산비고
→ 계산 결과와 근거
```

DAO는 **계산하지 않고 저장/조회만 함.**

---

## 7. 공제항목 DAO

DTO:

```text
(어떤 급여의) 공제
→ 급여번호

(국민연금/건강보험/장기요양/
 고용보험/소득세/지방소득세)
→ 공제종류
```

묶으면:

```text
[공제항목 DAO]

급여의 공제항목을 등록한다

특정 급여의 모든 공제항목을 조회한다

특정 급여의 국민연금을 조회한다

특정 급여의 건강보험을 조회한다

특정 급여의 고용보험을 조회한다

특정 급여의 소득세를 조회한다

특정 급여의 지방소득세를 조회한다

공제항목을 수정한다

공제항목을 삭제한다
```

결국:

```text
급여 + 공제종류
→ 특정 공제항목
```

이라는 조합.

---

## 8. 공제 계산근거 DAO

DTO:

```text
(어떤 공제항목의) 계산
→ 공제번호

(공제) 기준
→ 기준금액

(연도) 기준
→ 기준연도

(요율) 공제
→ 적용요율

(정액) 공제
→ 정액금액

(계산된) 공제
→ 최종 공제금액

(계산) 근거
→ 계산비고
```

인접한 것끼리 묶으면:

```text
[공제 계산근거 DAO]

공제항목의 계산근거를 등록한다

특정 공제항목의 계산근거를 조회한다

기준금액과 기준연도를 저장한다

적용된 공제요율을 저장한다

정액 공제금액을 저장한다

최종 공제금액을 저장한다

공제금액의 계산근거를 저장한다

기존 공제 계산근거를 수정한다
```

조합을 보면:

```text
기준금액 + 기준연도
→ 공제 계산 기준

적용요율 / 정액금액
→ 공제 방식

최종금액 + 계산비고
→ 공제 결과와 근거
```

---

## 9. 급여지급 DAO

DTO:

```text
(어떤 급여의) 지급
→ 급여번호

(실제) 지급
→ 지급금액

(언제) 지급
→ 지급일시

(어떤 방식) 지급
→ 지급방법

(지급/취소) 처리
→ 처리구분

(지급) 비고
→ 지급비고
```

묶으면:

```text
[급여지급 DAO]

급여 지급내역을 등록한다

급여 취소내역을 등록한다

특정 급여의 지급이력을 조회한다

특정 급여의 실제 지급금액을 조회한다

특정 급여의 최근 지급내역을 조회한다

지급일시와 지급방법을 조회한다

지급·취소 처리이력을 조회한다
```

여기서는:

```text
급여 + 지급/취소
→ 지급이력

금액 + 일시 + 방법
→ 실제 지급정보
```

이렇게 묶임.

---

그리고 네 미로보드에서 **DTO → DAO를 변환하는 규칙**을 진짜 간단하게 잡으면 이거임.

```text
DTO

(누구의)
(언제)
(무슨 종류)
(얼마)
(무슨 상태)

        ↓
인접한 데이터 결합

DAO

누구의 것을 조회한다
언제의 것을 조회한다
종류별로 조회한다
한 건을 등록한다
내용을 수정한다
이력을 조회한다
```

예를 들어:

```text
[DTO]

근로계약번호
+ 연도
+ 월

        ↓

[DAO]

특정 계약의 특정 월 급여를 조회한다
```

또:

```text
급여번호
+ 근태번호

        ↓

급여에 근태를 반영한다
```

또:

```text
급여번호
+ 공제종류

        ↓

특정 급여의 특정 공제항목을 조회한다
```

또:

```text
지급항목
+ 기준금액
+ 적용시간
+ 계산금액

        ↓

지급항목의 계산근거를 등록/조회한다
```

**이게 네가 찾던 “한 단계 위로 합치는 방법” 맞음.**

DTO가 가장 잘게 쪼갠 데이터라면, **그중 서로 의미상 붙어 있는 데이터 몇 개를 묶어서 DB에 어떤 요청을 할지 표현한 게 DAO**라고 생각하면 된다. 이제 미로보드에서 아래에서 위로 역조립할 수 있겠네. 인간도 결국 레고였다.
