맞아. 아까 우리가 쪼갠

> 계약한다 → 출근한다 → 연장/야간 판단한다 → 급여 계산한다 → 공제한다

이건 거의 **서비스단의 사람말**이야.

DAO까지 내려가면 훨씬 재미없어진다. DAO는 세상의 낭만을 모두 제거하고 결국 **“찾기 / 넣기 / 고치기 / 지우기 / 번호 뽑기”**만 한다.

그래서 9개 테이블을 **서비스 → DAO**까지 사람말로 쪼개면 이렇게 보면 됨.

---

### 1. 근로계약

```text
[서비스]

직원을 선택한다
↓
(월급/시급/일급) 급여형태를 정한다
↓
기준임금을 정한다
↓
(1일/주) 근로시간을 정한다
↓
계약기간을 판단한다
↓
(예정/진행중/종료) 상태를 정한다
↓
계약을 저장한다


[DAO]

새 계약번호를 만든다
↓
계약 한 건을 저장한다
↓
직원의 현재 계약을 찾는다
↓
직원의 과거 계약 목록을 찾는다
↓
계약 한 건을 찾는다
↓
계약 내용을 수정한다
↓
계약 상태를 변경한다
```

즉 서비스는 **“이 계약이 어떤 계약인가?”** 판단하고, DAO는 **“계약 행 하나 넣어줘”**만 함.

---

### 2. 직원 근태

```text
[서비스]

근무일을 확인한다
↓
(평일/휴일) 판단한다
↓
출근한다
↓
휴게시간을 반영한다
↓
(연장/야간) 근무시간을 계산한다
↓
퇴근한다
↓
(정상/결근/유급휴가/무급휴가) 판단한다
↓
근태를 기록한다


[DAO]

새 근태번호를 만든다
↓
근태 한 건을 저장한다
↓
특정 날짜 근태를 찾는다
↓
특정 직원의 월 근태를 전부 찾는다
↓
근태 한 건을 수정한다
↓
근태 한 건을 삭제한다
```

여기서 중요.

```text
야간 몇 시간인지 계산한다
연장 몇 시간인지 계산한다
```

이건 **서비스**.

DAO는 그냥:

```text
계산된 야간시간 저장
계산된 연장시간 저장
```

만 함.

---

### 3. 급여 반영 근태

```text
[서비스]

이번 급여의 대상 월을 확인한다
↓
그 달 근태를 가져온다
↓
급여에 반영할 근태인지 확인한다
↓
반영 대상 근태를 확정한다
↓
급여와 근태를 연결한다


[DAO]

새 급여반영근태 번호를 만든다
↓
급여번호 + 근태번호를 저장한다
↓
이 급여에 연결된 근태 목록을 찾는다
↓
이 근태가 이미 급여에 반영됐는지 찾는다
↓
급여와 근태의 연결을 삭제한다
```

얘 DAO의 핵심은 진짜 하나임.

> **“급여 #100에 근태 #51이 들어갔다”를 기록한다.**

그 이상 철학 없음.

---

### 4. 급여

```text
[서비스]

근로계약을 확인한다
↓
급여 대상 연/월을 정한다
↓
이미 생성된 급여인지 확인한다
↓
월 근태를 확인한다
↓
급여 산정을 시작한다
↓
지급항목을 계산한다
↓
공제를 계산한다
↓
급여를 확정한다
↓
지급되면 지급완료 처리한다


[DAO]

새 급여번호를 만든다
↓
급여 한 건을 저장한다
↓
계약 + 연도 + 월로 기존 급여를 찾는다
↓
급여번호로 한 건을 찾는다
↓
직원의 월별 급여 목록을 찾는다
↓
급여 상태를 변경한다
```

여기서도:

```text
월급 얼마?
연장수당 얼마?
```

DAO는 모름.

`Payroll DAO`는 그냥:

> “2026년 8월 급여 한 줄 만들었습니다.”

하고 퇴근함.

---

### 5. 지급항목

```text
[서비스]

이번 급여에 어떤 돈을 줄지 정한다
↓
(기본급/수당/기타) 분류한다
↓
(기본급/주휴/연장/야간 등) 항목을 만든다


[DAO]

새 지급항목 번호를 만든다
↓
지급항목 한 건을 저장한다
↓
특정 급여의 지급항목 목록을 찾는다
↓
특정 지급항목을 찾는다
↓
필요하면 지급항목을 수정하거나 삭제한다
```

예를 들어 서비스가:

> “연장수당이 필요하다.”

판단하면 DAO는:

> “연장수당이라는 행 하나 넣음.”

끝.

---

### 6. 지급 계산근거

```text
[서비스]

어떤 지급항목인지 확인한다
↓
기준금액 또는 기준시급을 정한다
↓
적용할 근무시간을 정한다
↓
금액을 계산한다
↓
계산 결과와 근거를 만든다


[DAO]

새 계산근거 번호를 만든다
↓
계산된 기준금액을 저장한다
↓
적용시간을 저장한다
↓
최종 지급금액을 저장한다
↓
계산비고를 저장한다
↓
특정 지급항목의 계산근거를 찾는다
```

여기는 특히 구분이 명확함.

```text
서비스
15,000 × 8 × 1.5 = 180,000이라고 계산

DAO
15,000 / 8 / 180,000을 DB에 저장
```

DAO한테 곱셈까지 시키지 마. 이미 SQL도 충분히 고생하고 있다.

---

### 7. 공제항목

```text
[서비스]

이번 급여에 필요한 공제를 판단한다
↓
(국민연금/건강보험/장기요양/고용보험/소득세/지방소득세)
공제항목을 만든다


[DAO]

새 공제번호를 만든다
↓
공제항목 한 건을 저장한다
↓
특정 급여의 공제항목 목록을 찾는다
↓
공제항목 한 건을 찾는다
↓
필요하면 수정하거나 삭제한다
```

`payroll_earning`의 반대편이라고 생각하면 거의 끝.

```text
earning = 줄 돈 이름
deduction = 뺄 돈 이름
```

---

### 8. 공제 계산근거

```text
[서비스]

공제항목을 확인한다
↓
공제 기준금액을 정한다
↓
기준연도를 확인한다
↓
(요율/정액/세액표) 방식을 판단한다
↓
공제금액을 계산한다
↓
계산근거를 만든다


[DAO]

새 공제계산 번호를 만든다
↓
기준금액을 저장한다
↓
적용요율을 저장한다
↓
정액공제금액을 저장한다
↓
최종 공제금액을 저장한다
↓
기준연도를 저장한다
↓
계산비고를 저장한다
↓
특정 공제항목의 계산근거를 찾는다
```

여기도 똑같음.

```text
서비스 = 얼마를 빼야 하는지 계산
DAO = 계산된 값을 기록
```

---

### 9. 급여 지급

```text
[서비스]

급여가 확정됐는지 확인한다
↓
실지급액을 계산한다
↓
지급정보를 확인한다
↓
급여를 지급 처리한다
↓
급여 상태를 지급완료로 바꾼다

또는

기존 지급을 확인한다
↓
지급을 취소한다
↓
취소 이력을 남긴다
↓
급여를 다시 확정 상태로 돌린다


[DAO]

새 지급내역 번호를 만든다
↓
지급내역을 저장한다
↓
급여의 지급내역 목록을 찾는다
↓
최근 지급내역을 찾는다
↓
취소내역을 저장한다
```

여기서는 **기존 지급 행을 삭제하지 않는 것**도 포인트임.

사람말로:

```text
9월 10일 지급
↓
9월 11일 취소
↓
9월 12일 재지급
```

을 전부 기록으로 남기는 거.

---

## 네 미로보드에 그릴 때는 이렇게 3단으로 그리면 됨

예를 들어 `직원근태` 하나만 그리면:

```text
┌────────────────────────┐
│       근태 서비스       │
│                        │
│ (평일/휴일) 판단        │
│      ↓                 │
│ 출근/퇴근 처리          │
│      ↓                 │
│ (연장/야간) 시간 계산   │
│      ↓                 │
│ 근태상태 판단           │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│        근태 DAO         │
│                        │
│ 새 번호 발급            │
│ 근태 등록               │
│ 날짜별 조회             │
│ 월별 목록 조회          │
│ 근태 수정               │
│ 근태 삭제               │
└──────────┬─────────────┘
           ↓
┌────────────────────────┐
│         근태 DTO        │
│                        │
│ 근태 한 행의 데이터     │
└────────────────────────┘
```

이 구조를 9개 전부에 똑같이 적용하면 됨.

그리고 전체를 한 문장으로 구분하면 꽤 중요함:

> **서비스는 “무엇을 해야 하는지 판단하고 계산”한다.  
> DAO는 “DB에서 무엇을 가져오고 무엇을 저장할지”만 담당한다.  
> DTO는 “DB에서 가져온 한 덩어리의 데이터”다.**

그래서 네 미로보드에서 **서비스 박스 안에는 `판단/계산/처리/확정`**, **DAO 박스 안에는 `번호발급/등록/조회/수정/삭제`** 같은 동사를 넣으면 층이 확실하게 갈린다. 이 기준 잡히면 그 무서웠던 배선도가 꽤 단순해짐.
