응. 아까 만든 **RequestVO → Service → DTO → DAO → DB → ResponseVO** 파이프라인에다가, 이번엔 옆에 **“오류 발생 시 행동강령”**을 붙이면 됨.

핵심은 **에러가 났다고 전체를 뒤집지 말고, 어느 층에서 끊겼는지부터 찾는 것**이다. 인간은 에러 하나 뜨면 일단 SecurityConfig부터 뜯는 습성이 있는데 그러지 말자.

```text
사용자
  ↓
RequestVO
  ↓
Controller
  ↓
Service
  ↓
DTO
  ↓
DAO
  ↓
Mapper SQL
  ↓
DB
```

오류가 나면 **반대 방향으로 추적**한다.

```text
에러 확인
↓
어느 계층에서 발생했는지 찾기
↓
그 계층의 입력값 확인
↓
바로 이전 단계 출력값 확인
↓
원인 수정
↓
같은 요청 다시 실행
```

### 1. RequestVO 단계에서 오류

예를 들어:

```text
직원번호 없음
급여 대상월 없음
출근시간 형식 이상
지급금액 음수
```

이 경우:

```text
RequestVO 오류
↓
요청값 확인
↓
필수값 누락 확인
↓
형식 확인
↓
범위 확인
↓
잘못됐으면 Service까지 보내지 않고 종료
```

사람말로:

> **“사용자가 보낸 값부터 정상인가?”**

여기서 막히면 DB 볼 필요 없음.

예:

```text
payrollYear = null
```

인데 SQL부터 뒤지고 있으면 컴퓨터가 아니라 사람이 디버깅 대상임.

---

### 2. Controller 단계에서 오류

주로:

```text
URL 잘못됨
HTTP Method 잘못됨
@RequestBody 누락
@PathVariable 이름 불일치
Validation 실패
로그인/권한 문제
```

행동강령:

```text
요청 URL 확인
↓
GET/POST/PATCH/DELETE 확인
↓
Controller 매핑 확인
↓
RequestVO가 제대로 들어왔는지 확인
↓
Service 호출 여부 확인
```

즉:

> **“요청 자체가 백엔드 입구를 통과했나?”**

---

### 3. Service 단계에서 오류

여기가 네 급여 프로젝트에서 제일 중요함.

예:

```text
계약이 존재하지 않음
종료된 계약
이미 같은 월 급여 존재
근태 없음
같은 근태 중복반영
급여가 산정중인데 지급 요청
지급완료 급여를 또 지급
```

행동강령:

```text
Service 입력 확인
↓
필요한 조회 결과 확인
↓
업무조건 확인
↓
계산 중간값 확인
↓
문제가 있으면 예외 발생
↓
DB 변경 중이었다면 전체 롤백
```

사람말:

> **“데이터는 정상인데 업무적으로 가능한 행동인가?”**

예를 들어:

```text
2026년 8월 급여 생성 요청
↓
이미 8월 급여 존재
↓
중복 생성 금지
↓
409 Conflict
```

이건 DB 오류가 아니라 **업무 오류**임.

---

### 4. 계산 오류

특히 근태/급여.

```text
출근 09:00
퇴근 20:00
휴게 60분
```

인데 연장시간이 이상하다면:

```text
원본 출근시간 확인
↓
원본 퇴근시간 확인
↓
휴게시간 확인
↓
소정근로시간 확인
↓
실근로시간 확인
↓
연장시간 확인
↓
야간시간 확인
```

급여도:

```text
기준시급
↓
적용시간
↓
가산조건
↓
계산금액
```

순서대로 본다.

예:

```text
연장수당 이상
```

이면 바로 최종금액부터 뜯지 말고:

```text
연장시간 맞음?
↓
기준시급 맞음?
↓
계산에 사용한 시간 맞음?
↓
계산금액 맞음?
```

이렇게 역추적.

---

### 5. DTO 단계에서 오류

주로:

```text
VO → DTO 변환하면서 값 빠짐
필드명 오타
자료형 불일치
Builder에서 필드 누락
```

행동강령:

```text
Service가 받은 값 확인
↓
생성된 DTO 확인
↓
DTO의 각 필드 확인
↓
DAO에 넘어간 DTO 확인
```

예를 들어 지금 네 DDL에 있었던:

```text
overtime_hoursn
```

같은 놈.

Java는:

```text
overtimeHours
```

인데 Mapper는:

```text
overtime_hoursn
```

이면 인류가 또 한 글자로 한 시간을 잃게 됨.

---

### 6. DAO 단계에서 오류

DAO에서 확인할 건 별거 없음.

```text
DAO 메서드가 실제 호출됐나?
↓
파라미터가 제대로 넘어왔나?
↓
Mapper의 statement id가 맞나?
↓
결과가 0건인가 / 여러 건인가?
```

사람말:

> **“DB에게 제대로 명령했나?”**

예:

```text
selectOne(payrollNo)
```

호출했는데 `payrollNo = null`이면 Mapper 욕하기 전에 호출부부터 봄.

---

### 7. Mapper / SQL 오류

여기는 로그가 왕임.

확인 순서:

```text
실행 SQL 확인
↓
바인딩 값 확인
↓
테이블명 확인
↓
컬럼명 확인
↓
WHERE 조건 확인
↓
JOIN 조건 확인
↓
DB에서 SQL 직접 실행
```

특히 MyBatis면:

```text
Invalid column
ORA-00942
ORA-00904
TooManyResultsException
BindingException
```

같은 메시지부터 읽는다.

**맨 마지막 줄만 보지 말고 `Caused by:` 첫 원인을 찾는 게 중요함.**

---

### 8. DB 제약조건 오류

네 구조에서는 꽤 자주 만날 것들:

```text
PK 중복
FK 없음
UNIQUE 중복
NOT NULL 누락
CHECK 위반
```

예:

```text
(contract_no, payroll_year, payroll_month)
```

UNIQUE 위반이면:

> 같은 계약의 같은 월 급여를 또 만들려고 했다는 뜻.

또:

```text
(payroll_no, emp_attendance_no)
```

UNIQUE 위반이면:

> 같은 급여에 같은 근태를 두 번 연결하려고 했다는 뜻.

이런 건 제약조건을 지우는 게 해결이 아님.

```text
제약조건 오류
↓
왜 중복 요청이 발생했는지 Service 확인
```

이게 맞음.

---

## 트랜잭션 오류 행동강령

급여 자동산정은 특히 중요함.

예:

```text
payroll 생성 성공
↓
payroll_attendance 20건 성공
↓
earning 생성 성공
↓
earning_calc 3번째에서 오류
```

여기서 앞의 데이터가 DB에 남으면 안 됨.

그래서 이 업무는:

```text
급여 생성
+
근태 반영
+
지급항목 생성
+
계산근거 생성
```

을 **하나의 작업**으로 묶고,

중간에 하나라도 실패하면:

```text
전부 롤백
```

이 원칙.

공제 계산도 마찬가지.

```text
공제항목 생성
+
공제계산근거 생성
```

중 하나 실패하면 전체 취소.

지급도:

```text
payment 이력 생성
+
payroll 상태 지급완료 변경
```

둘 중 하나만 성공하면 안 됨.

---

## HTTP 오류도 규칙 정해두면 편함

네 프로젝트에서는 대충 이렇게 잡으면 충분함.

| 상황 | 응답 |
|---|---:|
| 요청값 자체가 이상함 | `400 Bad Request` |
| 로그인 안 됨 | `401 Unauthorized` |
| 권한 없음 | `403 Forbidden` |
| 계약/급여/근태 없음 | `404 Not Found` |
| 이미 같은 월 급여 존재 | `409 Conflict` |
| 이미 반영한 근태를 또 반영 | `409 Conflict` |
| 확정되지 않은 급여 지급 등 상태 충돌 | `409 Conflict` |
| 예상 못한 서버/DB 오류 | `500 Internal Server Error` |

---

## 네가 실제 개발 중 에러 만나면 이 순서만 외워

```text
① 에러 메시지 읽기
        ↓
② 어느 계층에서 터졌는지 찾기
        ↓
③ 그 계층에 들어온 입력 확인
        ↓
④ 바로 전 단계의 출력 확인
        ↓
⑤ Mapper라면 실제 SQL + 바인딩값 확인
        ↓
⑥ DB 제약조건 확인
        ↓
⑦ 계산이면 중간값부터 확인
        ↓
⑧ 수정 후 같은 요청 재현
```

그리고 **하지 말아야 할 행동**은 명확함.

```text
에러 발생
↓
아무 클래스나 수정 ❌
↓
안 되니까 테이블 구조 변경 ❌
↓
안 되니까 Security 끄기 ❌
↓
안 되니까 예외 catch해서 무시 ❌
```

가장 중요한 한 문장으로 압축하면:

> **오류가 난 지점의 입력을 확인하고, 그 입력을 만들어낸 바로 이전 단계의 출력까지 거슬러 올라간다.**

이 방식이면 네가 지금 만든 파이프라인하고 완전히 연결돼.

```text
RequestVO
↓
Service
↓
DTO
↓
DAO
↓
DB
```

정상일 땐 아래로 따라가고, **오류일 땐 위로 역추적한다.**

이걸 미로보드 옆에 `정상 흐름 ↓ / 오류 추적 ↑` 두 화살표로 그려두면 꽤 쓸모 있는 개발 행동강령이 된다.
