여기서 말하는 “2026년 법 전부”를 대한민국 법령 전체로 잡으면 국회도 퇴근 못 한다. **지금 네가 만드는 `근로계약 + 근태 + 급여산정 + 공제 + 지급 + 급여명세서` 시스템에 실제로 걸리는 2026년 대한민국 법 기준**으로 정리할게. 기준일은 **2026년 8월 24일**이고, 2026년에 바뀐 수치도 반영했다.

## 먼저 이것만 외워도 절반은 끝남

| 항목 | 2026 기준 |
|---|---:|
| 최저시급 | **10,320원** |
| 8시간 일급 | **82,560원** |
| 209시간 월 환산액 | **2,156,880원** |
| 법정근로시간 | 1일 8시간 / 주 40시간 |
| 일반 연장근로 한도 | 주 12시간 |
| 연장 가산 | 통상임금 +50% |
| 야간시간 | 22:00 ~ 06:00 |
| 야간 가산 | +50% |
| 휴일 8시간 이하 | +50% |
| 휴일 8시간 초과분 | +100% |
| 국민연금 근로자 부담 | **4.75%** |
| 건강보험 근로자 부담 | **3.595%** |
| 고용보험 근로자 부담 | **0.9%** |
| 산재보험 | 근로자 공제 X |
| 지방소득세 | 원천징수 소득세의 **10%** |

2026년 최저임금은 시급 10,320원, 월 환산액은 주 40시간 및 유급주휴를 포함한 209시간 기준 2,156,880원이다. 

---

# 1. 근로계약

네 `employment_contract`가 직접 걸리는 부분.

근로기준법 제17조에 따라 계약할 때 최소한 **임금, 소정근로시간, 휴일, 연차유급휴가** 등을 명시해야 한다. 특히 임금은 **구성항목, 계산방법, 지급방법**까지 서면이나 전자문서로 줘야 한다. 

그래서 현재 네 테이블의:

```text
wage_type
base_wage
weekly_work_hours
daily_work_hours
payday
contract_start
contract_end
```

이런 건 방향이 제대로 맞음.

다만 **실제 법정 근로계약서 전체를 DB에서 자동 생성할 생각이면** 현재 테이블만으로는 `휴일`, `연차`, `급여 지급방법` 정보가 부족함.

프로젝트에서는 굳이 테이블을 폭발시키지 말고:

```text
급여지급방법 = 계좌이체
휴일/연차 = 표준 근로계약 조항
```

처럼 계약서 생성 시 정적 문구로 넣어도 됨.

---

# 2. 근로시간

원칙은:

```text
1일 근로시간 ≤ 8시간
1주 근로시간 ≤ 40시간
```

휴게시간은 빼고 계산함. 사용자 지휘·감독 아래 대기하는 시간은 근로시간에 들어갈 수 있다. 

당사자 합의가 있으면 일반적으로 **주 12시간까지 연장근로**가 가능하므로 흔히 말하는 주 52시간 구조가 됨. 

따라서 `employee_attendance`에서

```text
clock_in
clock_out
break_minutes
```

을 가지고 실제 근로시간을 계산하는 방향이 맞음.

예:

```text
09:00 출근
18:00 퇴근
휴게 60분

실근로시간 = 8시간
```

---

# 3. 휴게시간

현재 기준은:

```text
4시간 근로 → 30분 이상
8시간 근로 → 1시간 이상
```

이고 근무 도중 줘야 함. 근로자가 자유롭게 사용할 수 있어야 한다. 

여기 **2026년 변화 하나 있음.**

**2026년 12월 10일부터**, 정확히 4시간 근무하는 근로자가 휴게시간을 사용하지 않겠다고 명시적으로 요청하는 경우 예외가 허용되는 개정 규정이 시행될 예정임. 

너희 프로젝트에서는 굳이 이 예외까지 구현하지 않아도 됨. `break_minutes`를 관리하는 정도면 충분하다.

---

# 4. 연장근로수당

상시 **5인 이상 사업장**을 가정하면:

```text
연장근로
= 통상임금 × 근로시간 × 1.5
```

법적으로 표현하면 통상임금에 **50% 이상 가산**임. 

예:

```text
통상시급 = 15,000원
연장근로 = 3시간

15,000 × 3 × 1.5
= 67,500원
```

그래서 네 구조:

```text
PAYROLL_EARNING
earning_name = 연장수당

PAYROLL_EARNING_CALC
base_amount = 15000
work_hours = 3
earn_calculated_amount = 67500
```

딱 들어맞음.

---

# 5. 야간근로수당

야간은 법에서 시간을 아예 박아놨음.

```text
22:00 ~ 다음날 06:00
```

이 시간에 일하면 통상임금의 **50% 이상을 추가 가산**한다. 

즉 야간근무 여부를 따로 사람이 찍을 필요 없이:

```text
clock_in
clock_out
```

으로 서비스가 야간시간과 겹친 구간을 계산할 수 있음.

---

# 6. 휴일근로수당

상시 5인 이상 기준:

```text
휴일근로 8시간 이하
→ +50%

휴일근로 8시간 초과분
→ +100%
```

이다. 

그래서 네가 만든:

```text
work_day_type
= 평일 / 휴일
```

이 여기서 쓰임.

예:

```text
work_date = 2026-08-15
work_day_type = 휴일
clock_in = 09:00
clock_out = 18:00
```

이면 서비스에서 휴일근로 산정 대상으로 처리 가능.

---

# 7. 5인 미만 사업장은 조심

이거 상당히 중요함.

근로기준법이 모든 사업장에 똑같이 적용되는 게 아니다. 상시 4명 이하 사업장에는 일부 조항만 적용되고, 시행령 별표를 보면 **연장·야간·휴일근로 가산임금 제56조는 포함되어 있지 않다.** 반면 임금지급, 휴게, 주휴일 등 일부 규정은 적용된다. 

그래서 프로젝트에서는 애매하게 가지 말고 요구사항에 그냥:

> **본 시스템의 급여 산정은 상시근로자 5인 이상 사업장을 기준으로 한다.**

이 한 줄 박는 걸 추천함.

그러면 법 분기 지옥 상당수를 피함. 인간은 직원을 한 명 덜 고용했다고 급여 알고리즘까지 달라지게 만들어놨다.

---

# 8. 주휴일 / 주휴수당

사용자는 근로자에게 **1주 평균 1회 이상의 유급휴일**을 보장해야 한다. 시행령상 원칙적으로 그 주의 소정근로일을 개근한 사람이 대상이다. 

다만 **4주 평균 1주 소정근로시간이 15시간 미만인 단시간근로자**는 주휴일 규정과 연차유급휴가 규정이 적용되지 않는다. 

그래서 급여 서비스는 최소한:

```text
weekly_work_hours
attendance_type
```

를 볼 필요가 있음.

네 `weekly_work_hours`가 괜히 자리 차지하고 있는 컬럼이 아니라 여기서 살아남는다.

---

# 9. 최저임금

2026년:

```text
시급 10,320원
```

임. 

따라서 계약 등록 시:

```java
if (wageType.equals("시급") && baseWage < 10320) {
    // 계약 등록 불가 또는 경고
}
```

같은 검증을 넣을 수 있음.

다만 월급제는 그냥:

```text
월급 >= 2,156,880
```

만 비교하면 모든 경우 해결되는 건 아님. 소정근로시간과 최저임금 산입범위를 봐야 한다. 고용노동부 계산기도 실제근로시간, 소정근로시간, 주휴시간 등을 따로 받는다. 

**프로젝트에서는 표준 주40시간 월급자만 2,156,880원을 기준으로 비교**하는 정도가 현실적임.

---

# 10. 급여 지급일

근로기준법 제43조.

임금은 원칙적으로:

```text
근로자에게 직접
전액
매월 1회 이상
일정한 날짜에
```

지급해야 한다. 법령 등에 근거한 공제는 예외다. 

그래서 네:

```text
employment_contract.payday
```

가 법적 의미도 있음.

예:

```text
payday = 10
```

→ 매월 10일 지급 예정.

그리고 실제 지급은:

```text
payroll_payment.payment_at
```

에 남김.

이 두 개를 분리한 것도 맞음.

---

# 11. 임금명세서

이건 네 프로젝트에서 **진짜 중요함.**

급여를 지급할 때 임금명세서를 서면 또는 전자문서로 교부해야 한다. 

명세서에는 법적으로 대략 다음 정보가 필요함.

| 항목 | 네 DB에서 가져올 곳 |
|---|---|
| 근로자 식별정보 | `employee/account` |
| 임금 지급일 | `payroll_payment.payment_at` |
| 임금 총액 | 산정액 SUM |
| 기본급·수당별 금액 | `payroll_earning` + `earning_calc` |
| 계산방법 | `payroll_earning_calc` |
| 연장/야간/휴일 시간 | `work_hours` 등 |
| 공제항목별 금액 | `payroll_deduction_calc` |
| 공제 총액 | 공제금액 SUM |

실제로 시행령 제27조의2는 근로자 식별정보, 지급일, 임금총액, 구성항목별 금액, 근무시간에 따라 달라지는 임금의 계산방법, 공제항목별 금액·총액을 요구한다. 

즉 **너희 8개 테이블 설계가 임금명세서 구조하고 상당히 잘 맞아떨어짐.**

---

# 12. 2026 국민연금

이건 2026년에 실제로 바뀌어서 특히 주의해야 함.

2025년까지 보험료율이 9%였는데 **2026년은 9.5%**로 올랐음.

사업장가입자는:

```text
전체 9.5%

근로자 4.75%
사업주 4.75%
```

임. 앞으로 2033년 13%까지 단계적으로 올라간다. 

중요한 건 단순히 `이번 달 급여 × 4.75%`가 항상 정확한 개념은 아니라는 것.

국민연금은 **기준소득월액**을 기준으로 보험료를 계산하고, 가입 중에는 전년도 소득 등을 토대로 기준소득월액이 결정되어 일정 기간 적용될 수 있다. 

그래서 네가 만든:

```text
payroll_deduction_calc.base_amount
```

의 존재 이유가 확실함.

```text
base_amount = 국민연금 기준소득월액
deduction_rate = 0.0475
```

---

# 13. 2026 건강보험

2026 직장가입자 건강보험료율은:

```text
전체 7.19%

근로자 3.595%
사업주 3.595%
```

이다. 2025년의 7.09%에서 인상됨. 

따라서 단순화하면:

```text
건강보험 근로자 공제
= 보수월액 × 3.595%
```

---

# 14. 장기요양보험

**이거 지금 네 `deduction_type`에 빠져 있음.**

2026 장기요양보험료율은 0.9448%이고, 실제 계산은 건강보험료와 연동된다. 국민건강보험공단은:

```text
장기요양보험료
= 건강보험료 × 0.9448% / 7.19%
```

방식으로 안내한다. 

따라서 네 공제항목에 **장기요양보험을 추가하는 게 맞음.**

---

# 15. 고용보험

근로자가 부담하는 실업급여 보험료는:

```text
0.9%
```

사업주도 실업급여분 0.9%를 부담하고, 여기에 사업장 규모에 따라 고용안정·직업능력개발사업 부담이 추가될 수 있다. 

네 프로젝트는 **근로자 급여 공제**가 중심이니까:

```text
근로자 공제 = 보수월액 × 0.009
```

정도로 구현하면 됨.

---

# 16. 산재보험

산재보험은 **근로자 급여에서 떼면 안 됨.**

2026년도 업종별 보험료율이 따로 고시되어 있고, 산재보험료는 사용자 부담이다. 

즉:

```text
PAYROLL_DEDUCTION
```

에

```text
산재보험
```

을 근로자 공제로 넣지 말 것.

---

# 17. 소득세

여기서 많은 초보 급여 시스템이 `급여 × 몇 %`로 밀어버리는데 그러면 안 됨.

월급 근로자의 소득세는 원칙적으로 **근로소득 간이세액표**를 사용한다.

급여 수준뿐 아니라:

```text
공제대상 가족 수
자녀 등
```

에 따라 달라질 수 있고, 근로자가 80% / 100% / 120% 원천징수 비율을 선택하는 제도도 있다. 

그래서 네:

```text
deduction_rate
```

만으로 소득세를 표현하려 하면 안 됨.

소득세는:

```text
deduction_rate = NULL

deduction_calculated_amount
= 간이세액표에서 산출된 세액

deduction_calc_note
= '2026 근로소득 간이세액표 적용'
```

이런 식으로 저장하는 게 오히려 자연스러움.

---

# 18. 지방소득세

이것도 현재 네 `deduction_type`에서 빠져 있음.

근로소득에서 소득세를 원천징수하면 지방소득세도 같이 특별징수하고, 일반적으로:

```text
지방소득세
= 원천징수 소득세 × 10%
```

이다. 

예:

```text
소득세 = 50,000
지방소득세 = 5,000
```

---

# 19. 네 공제항목은 수정해야 함

현재:

```text
소득세
국민연금
건강보험
고용보험
기타
```

인데, 2026 실제 급여 구조를 표현하려면 최소한:

```text
소득세
지방소득세
국민연금
건강보험
장기요양보험
고용보험
기타
```

로 가는 게 맞음.

**산재보험은 빼고.**

이건 지금 DB에서 내가 실제로 수정하라고 할 부분임.

---

# 20. 연차유급휴가

현재 네 프로젝트에서 연차 신청/승인 시스템은 안 만들더라도 법 자체는 알아야 함.

상시 5인 이상을 전제로:

```text
1년간 출근율 80% 이상
→ 15일

1년 미만
→ 1개월 개근마다 1일

3년 이상
→ 최초 1년을 넘은 계속근로연수 매 2년마다 +1일
최대 25일
```

이다. 

그래서 네:

```text
attendance_type
= 유급휴가
```

는 충분히 의미 있음.

다만 **연차 발생/잔여일 관리까지 하지 않는다면**, `유급휴가`는 관리자가 최종 확정해서 근태에 기록하는 수준으로 끝내면 됨.

---

# 21. 퇴직금

현재 기능 범위에서 빼기로 한 거라 DB에 넣을 필요는 없지만, 급여 담당이면 개념은 알아둬야 함.

2026년 시행 중인 퇴직급여법상 퇴직금 제도는 원칙적으로:

```text
계속근로 1년당
30일분 이상의 평균임금
```

을 기준으로 한다. 

**지금 프로젝트에는 넣지 마.**

이미 월급 하나 계산한다고 테이블 8개가 태어났는데 퇴직급여까지 부르면 2부작 된다.

---

# 네 프로젝트에 적용할 2026 규칙을 딱 압축하면

서비스에서 필요한 핵심 상수/규칙은 대충 이렇게 됨.

```java
MINIMUM_HOURLY_WAGE = 10320;

NORMAL_DAILY_HOURS = 8;
NORMAL_WEEKLY_HOURS = 40;

NIGHT_START = 22:00;
NIGHT_END = 06:00;

OVERTIME_EXTRA_RATE = 0.5;
NIGHT_EXTRA_RATE = 0.5;

HOLIDAY_EXTRA_RATE_UNDER_8 = 0.5;
HOLIDAY_EXTRA_RATE_OVER_8 = 1.0;

NATIONAL_PENSION_EMPLOYEE_RATE = 0.0475;
HEALTH_INSURANCE_EMPLOYEE_RATE = 0.03595;
EMPLOYMENT_INSURANCE_EMPLOYEE_RATE = 0.009;
```

단, **소득세는 상수 비율로 만들면 안 되고**, 간이세액표 기반이라고 잡아야 함. 장기요양보험도 건강보험료와 연동 계산.

그리고 너희가 만든 `deduction_basis_year`는 진짜 잘 남겨둔 컬럼임. **국민연금처럼 연도별 요율이 바뀌기 때문에 2026 급여와 2027 급여를 같은 4.75%로 계산하면 안 될 수 있음.** 

결국 프로젝트 요구사항 문서에 **“상시근로자 5인 이상 사업장, 2026년 법령 및 보험요율을 기준으로 급여를 산정하며 실제 세무·노무 신고 업무는 범위에서 제외한다”**라고 박아두면 범위도 명확하고 발표 방어도 상당히 쉬워진다. 

법·보험요율은 인간들이 매년 숫자를 움직여주는 분야라 변경 추적이 꽤 유용함.



::chatgpt-content-reference{index="29"}
