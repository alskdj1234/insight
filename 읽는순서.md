다 봤음. **어제 만든 현재 남아 있는 파일들 기준으로는 그냥 파일 생성 순서대로 읽으면 별로고, “업무 이해 → 데이터 → DB 행동 → 업무 행동 → 데이터 흐름 → VO → 전체 조립 → 구현/디버깅” 순서가 제일 좋음.**

그리고 `서비스.md`는 어제 만들었다가 삭제했고, `서비스2.md`는 지금 `서비스 도출과정.md`로 이름 바뀐 상태라 **현재 저장소에 남은 버전 기준**으로 잡았음. 저장소 루트에도 `dao.md`, `dto.md`, `service.md`, `vo.md`, `vo가공 전 단계.md`, `서비스 도출과정.md`, `요령1~3`, `개요.md`, `오류 났을때.md`, `관련 법령.md`가 남아 있음. 

### 내가 추천하는 읽는 순서

**0. `개요.md` 5분만 훑기**

처음부터 정독하지 말고 **전체 지도만 본다.**

여기 핵심은:

```text
RequestVO
↓
Service
↓
DTO
↓
DAO
↓
DB
```

그리고 조회는 반대라는 것. 이 파일 자체가 근로계약부터 명세서까지 전체 파이프라인을 한데 묶어놓은 문서라, 처음에는 지도 역할로만 쓰는 게 좋음. 

---

**1. `관련 법령.md`**

먼저 **왜 이런 데이터를 저장하고 왜 이런 계산을 하는지**부터 봐.

여기서:

- 계약에 왜 임금/근로시간이 필요한지
- 연장/야간/휴일이 왜 갈리는지
- `weekly_work_hours`가 왜 필요한지
- 지급일을 왜 계약과 실제 지급으로 나누는지
- 급여명세서에 왜 계산근거가 필요한지

를 이해함. 

즉 **업무 규칙의 원인**부터 보는 거.

---

**2. `서비스 도출과정.md`**

그다음 이거.

이게 사실 급여 공부의 **사람말 버전 시작점**임.

```text
계약
→ 근태
→ 급여반영근태
→ 급여
→ 지급/공제
→ 실제 지급
```

그리고 각 테이블을 `(연장) 근무`, `(야간) 수당`, `(국민) 연금` 식으로 인간 언어로 쪼개놓음. 

마지막에는 9개를 하나의 전체 흐름으로 합쳐놓기도 했음. 

**여기까지는 코드 생각하지 말고 업무만 이해.**

---

**3. `dto.md`**

이제 사람말을 **실제 데이터 단위로 내리는 단계**.

```text
큰 업무
→ 의미 단위
→ 실제 저장 데이터
```

예:

```text
(연장) 근무
→ 연장 근무시간

(계약상) 임금
→ 기준임금
```

이런 식. 

그리고 9개 테이블 전체가 이 규칙으로 정리되어 있음. 

여기서 머릿속에 박을 건:

> **DTO = 가장 작은 실제 정보**

---

**4. `dao.md`**

DTO를 이해했으면 DAO.

이 문서가 정확히:

> DTO에서 의미상 붙어 있는 데이터를 묶어서 DB에서 할 행동을 만든다

로 시작함. 

예:

```text
급여번호 + 근태번호
→ 급여에 근태를 반영한다

계약번호 + 연도 + 월
→ 특정 월 급여를 조회한다
```

그리고 마지막에 핵심을 다시 박음.

```text
DTO = 데이터
↓
의미상 결합
DAO = DB 행동
```



---

**5. `service.md`**

이제 DAO보다 한 단계 올라감.

이 문서의 핵심은:

```text
DTO → 데이터
DAO → DB 행동
Service → DAO 행동들을 묶은 실제 업무
```

임. 

예를 들어 DAO 여러 개가:

```text
현재 계약 조회
계약조건 조회
계약 등록
계약 상태 변경
```

이라면 Service에서는:

```text
직원의 근로조건을 확인하고
새로운 근로계약을 체결·관리한다
```

가 됨.

결국 마지막에:

> **DTO → DAO는 데이터끼리 결합, DAO → Service는 행동끼리 결합**

이라고 정리돼 있음. 

여기까지 읽으면 네 백엔드 계층 구조가 거의 잡힘.

---

**6. `vo가공 전 단계.md`**

이제 좀 난이도 올라감.

서로 다른 DTO의 필드를 **인과관계상 같은 높이**로 묶음.

```text
소속
↓
시간/기간
↓
종류
↓
기준
↓
적용량
↓
결과
↓
상태
↓
근거
```

예를 들어 지급계산과 공제계산을:

```text
기준금액      ↔ 기준금액
적용시간      ↔ 적용요율
지급금액      ↔ 공제금액
계산비고      ↔ 계산비고
```

처럼 비교함. 

**이걸 DTO보다 먼저 읽으면 머리가 꼬임. 반드시 DTO/DAO/Service 뒤에.**

---

**7. `요령1.md`**

그 데이터를 이제 **입력 / 중간값 / 출력**으로 봄.

핵심:

```text
원천 입력
↓
중간 출력 = 다음 단계 입력
↓
중간 출력 = 다음 단계 입력
↓
최종 출력
```

예:

```text
출퇴근
↓
연장/야간시간
↓
연장/야간수당
↓
실지급액
```

앞 단계 출력이 바로 다음 단계 입력이 된다는 걸 제대로 설명한 파일임. 

이거 이해하면 서비스끼리 데이터가 어떻게 흘러가는지가 보임.

---

**8. `요령2.md`**

`요령1에서 만든 데이터 흐름을 그래서 코드에서 어디다 쓰는데?`에 답하는 파일.

실제로:

- VO 설계
- Service 경계
- DAO가 가져올 데이터
- 구현 순서
- 디버깅

에 어떻게 사용하는지 정리돼 있음. 

여기까지 오면 설계가 그냥 그림이 아니라 **코드 작성 지침**으로 바뀜.

---

**9. `vo.md`**

여기서 드디어 VO.

처음부터 명확하게:

```text
입력 VO = 사용자가 넣는 값
중간 VO = 앞 단계 출력이면서 다음 계산 입력
출력 VO = 화면에 보여줄 값
```

으로 나눔. 

그리고 실제로:

```text
EmploymentContractSaveRequestVO
AttendanceCalculationResultVO
PayrollCalculationContextVO
PayrollSettlementVO
PayrollStatementResponseVO
```

같은 후보까지 이어짐. 최종적으로 전체 VO 파이프라인도 정리돼 있음. 

**VO는 이때 읽어야 제일 잘 이해됨.**  
DTO 읽자마자 VO 읽으면 둘이 또 형제처럼 보여서 헷갈림.

---

**10. 다시 `개요.md` 정독**

이제 처음에 훑었던 개요를 다시 읽어.

처음에는 외계어였던:

```text
RequestVO
→ Service
→ DTO
→ DAO
→ DB
```

가 이때는 전부 의미가 생김.

그리고 파일 마지막에서 **각 기능을 세로로 하나씩 완성하라**는 실제 개발 방식까지 연결함.  

개요는 **처음 1번 + 마지막 1번** 읽는 게 좋음.

---

**11. `요령3.md`**

이건 공부 문서라기보다 **이제 뭘 만들지 실행 순서표**임.

파일 자체가:

```text
1. DDL 수정
2. DTO
3. DAO
4. 근로계약 하나 완성
5. 근태 하나 완성
6. payroll
...
```

순으로 되어 있고, 특히:

```text
❌ DTO 9개 → DAO 9개 → Service 9개

✅ 근로계약 하나 세로로 완성
   ↓
   근태 하나 세로로 완성
   ↓
   급여
```

을 강조함. 

즉 **공부 다 끝나고 코드 시작 직전에 보는 체크리스트**.

---

**12. `오류 났을때.md`**

맨 마지막.

왜냐면 이건 전체 파이프라인을 알아야 제대로 써먹음.

```text
정상 흐름
RequestVO
↓
Controller
↓
Service
↓
DTO
↓
DAO
↓
Mapper
↓
DB

오류 발생
↑ 역으로 추적
```

으로 되어 있음. 

Service 오류, 계산 오류, DTO 오류, Mapper 오류, DB 제약, 트랜잭션까지 전부 **어디서 끊겼는지를 역추적하는 방식**이라 구현 시작하고 옆에 켜두는 문서임.

---

### 진짜 압축하면

이 순서로 가.

```text
[개요 훑기]

1. 관련 법령
2. 서비스 도출과정
3. dto
4. dao
5. service
6. vo가공 전 단계
7. 요령1
8. 요령2
9. vo
10. 개요 정독
11. 요령3
12. 오류 났을때
```

그리고 **가장 중요한 구간은 2 → 3 → 4 → 5**임.

```text
사람이 하는 업무
        ↓
DTO: 어떤 데이터가 필요한가
        ↓
DAO: 그 데이터로 DB에서 뭘 하는가
        ↓
Service: 그 DB 행동들을 어떻게 업무로 묶는가
```

이 네 개가 이어져서 보이면 어제 했던 설계의 핵심은 거의 먹은 거임.

`vo가공 전 단계`, `요령1`, `요령2`는 그다음 **“이 데이터들이 서로 어떻게 흘러가냐”**를 이해하기 위한 심화편이고, `vo.md`는 그 흐름을 객체로 포장하는 단계.

**오늘 만든 `ddl.sql`, `ddl 구조도.md`는 이 학습 순서에서는 `관련 법령 → 서비스 도출과정` 사이에 참고용으로 끼워보면 됨.** 정독할 필요는 없고, “아 이 사람이 말한 근태번호가 실제로 이 FK구나” 확인하는 사전처럼 쓰는 게 맞음. 인간은 결국 자신이 어제 만든 문서를 오늘 다시 공부해야 하는 생물인데, 적어도 이번 문서들은 서로 연결은 잘 돼 있다.
