맞아. 이제 한 단계 더 위로 올라가면 됨.

지금까지가:

```text
DTO
→ 잘게 쪼갠 실제 데이터

DAO
→ 인접한 데이터를 묶어서
  등록/조회/수정/삭제하는 DB 행동
```

이었다면,

**서비스는 인접한 DAO 행동들을 다시 묶어서 “업무 하나”로 만드는 단계**라고 보면 됨.

---

### 1. 근로계약 서비스

DAO 쪽에서:

```text
직원의 현재 계약 조회
+ 과거 계약 조회
+ 급여조건 조회
+ 근로시간조건 조회
+ 계약 등록/수정
+ 계약상태 변경
```

이걸 인접한 것끼리 묶으면:

```text
[근로계약 서비스]

직원의 기존 계약을 확인한다
        ↓
새 근로조건을 확인한다
        ↓
(월급/시급/일급) 급여조건을 설정한다
        ↓
(1일/주) 근로시간을 설정한다
        ↓
계약기간을 설정한다
        ↓
(예정/진행중/종료) 계약상태를 판단한다
        ↓
근로계약을 등록하거나 변경한다
```

한 문장:

> **직원의 근로조건을 확인하고 새로운 근로계약을 체결·관리한다.**

---

### 2. 직원근태 서비스

DAO:

```text
날짜별 근태 조회
+ 월별 근태 조회
+ 출퇴근 정보 저장
+ 연장/야간 시간 저장
+ 근태상태 저장
+ 근태 수정
```

묶으면:

```text
[직원근태 서비스]

근무할 날짜를 확인한다
        ↓
(평일/휴일) 근무일을 판단한다
        ↓
출근과 퇴근을 기록한다
        ↓
휴게시간을 반영한다
        ↓
실제 근무시간을 계산한다
        ↓
(연장) 근무시간을 계산한다
        ↓
(야간) 근무시간을 계산한다
        ↓
(정상/결근/유급휴가/무급휴가)
근태상태를 처리한다
        ↓
근태기록을 저장한다
```

한 문장:

> **직원의 하루 근무기록을 바탕으로 실제 근태를 계산하고 관리한다.**

---

### 3. 급여반영근태 서비스

DAO:

```text
급여에 근태 연결
+ 급여별 반영근태 조회
+ 기존 반영여부 조회
+ 연결 해제
```

묶으면:

```text
[급여반영근태 서비스]

이번 급여의 대상기간을 확인한다
        ↓
해당 기간의 근태를 가져온다
        ↓
이미 다른 방식으로 반영됐는지 확인한다
        ↓
급여에 사용할 근태를 확정한다
        ↓
근태를 해당 급여에 반영한다
```

한 문장:

> **이번 월 급여 계산에 사용할 근태기록을 확정한다.**

이게 `payroll_attendance`의 존재 이유를 가장 사람답게 표현한 문장임.

---

### 4. 급여 서비스

DAO:

```text
계약별 월 급여 조회
+ 기존 급여 조회
+ 급여 생성
+ 상태별 급여 조회
+ 급여상태 변경
```

묶으면:

```text
[급여 서비스]

근로계약을 확인한다
        ↓
급여 대상 연·월을 확인한다
        ↓
기존 급여가 있는지 확인한다
        ↓
월 급여를 생성한다
        ↓
근태반영 결과를 확인한다
        ↓
지급항목과 공제를 반영한다
        ↓
급여를 확정한다
        ↓
지급 후 지급완료 처리한다
```

한 문장:

> **근로계약과 월 근태를 기준으로 한 달의 급여를 생성하고 상태를 관리한다.**

---

### 5. 지급항목 서비스

DAO:

```text
급여별 지급항목 조회
+ 기본급 조회
+ 수당 조회
+ 지급항목 등록/수정/삭제
```

묶으면:

```text
[지급항목 서비스]

이번 급여에서 지급할 항목을 확인한다
        ↓
(기본급/수당/기타)로 분류한다
        ↓
기본급을 생성한다
        ↓
필요한 수당을 판단한다
        ↓
(주휴/연장/야간 등) 수당을 생성한다
```

한 문장:

> **이번 급여에서 어떤 명목으로 돈을 지급할지 결정한다.**

---

### 6. 지급 계산근거 서비스

DAO:

```text
기준금액 저장
+ 적용시간 저장
+ 계산금액 저장
+ 계산근거 저장
+ 계산근거 조회
```

묶으면:

```text
[지급 계산근거 서비스]

지급할 항목을 확인한다
        ↓
계산에 사용할 기준금액을 정한다
        ↓
적용할 근무시간을 가져온다
        ↓
지급금액을 계산한다
        ↓
계산결과를 만든다
        ↓
왜 이 금액이 나왔는지 근거를 남긴다
```

예를 들어:

```text
(연장) 수당
        ↓
(기준) 시급 확인
        ↓
(연장) 시간 확인
        ↓
연장수당 계산
        ↓
최종금액 + 계산근거 저장
```

한 문장:

> **각 지급항목의 실제 지급금액과 그 계산 이유를 만든다.**

---

### 7. 공제항목 서비스

DAO:

```text
급여별 공제항목 조회
+ 특정 공제 조회
+ 공제항목 등록/수정/삭제
```

묶으면:

```text
[공제항목 서비스]

이번 급여에 필요한 공제를 확인한다
        ↓
(국민연금/건강보험/장기요양보험/
 고용보험/소득세/지방소득세)
적용여부를 판단한다
        ↓
필요한 공제항목을 생성한다
```

한 문장:

> **이번 급여에서 어떤 명목으로 돈을 공제할지 결정한다.**

`지급항목 서비스`와 정확히 대칭임.

---

### 8. 공제 계산근거 서비스

DAO:

```text
기준금액 저장
+ 기준연도 저장
+ 요율 저장
+ 정액금액 저장
+ 최종공제액 저장
+ 계산근거 저장
```

묶으면:

```text
[공제 계산근거 서비스]

공제항목을 확인한다
        ↓
공제 기준금액을 정한다
        ↓
적용할 기준연도를 확인한다
        ↓
(요율/정액/세액기준)
계산방식을 판단한다
        ↓
공제금액을 계산한다
        ↓
최종 공제금액을 만든다
        ↓
공제 계산근거를 남긴다
```

한 문장:

> **각 공제항목에서 실제로 얼마를 뺄지 계산하고 근거를 만든다.**

---

### 9. 급여지급 서비스

DAO:

```text
지급이력 등록
+ 지급이력 조회
+ 최근 지급내역 조회
+ 취소이력 등록
```

묶으면:

```text
[급여지급 서비스]

급여가 확정됐는지 확인한다
        ↓
최종 실지급금액을 확인한다
        ↓
지급방법과 지급일시를 확인한다
        ↓
실제 지급을 처리한다
        ↓
지급이력을 남긴다
        ↓
급여를 지급완료 처리한다
```

취소라면:

```text
기존 지급내역 확인
        ↓
취소 가능한 지급인지 확인
        ↓
지급취소 처리
        ↓
취소이력 기록
        ↓
급여상태를 다시 확정으로 변경
```

한 문장:

> **확정된 급여를 실제로 지급하거나 기존 지급을 취소하고 이력을 관리한다.**

---

그리고 네가 지금 만든 방식을 **아래에서 위로 보면** 딱 이렇게 됨.

```text
DTO
출근시각
퇴근시각
휴게시간
연장시간
야간시간

        ↓ 인접 데이터 묶기

DAO
출퇴근 근태 저장
연장·야간 근무시간 저장
월 근태 조회

        ↓ 인접 DAO 행동 묶기

SERVICE
출퇴근 기록을 바탕으로
실제 근무시간과 연장·야간근무를 계산하고
하루 근태를 처리한다
```

또:

```text
DTO
급여번호
근태번호

        ↓

DAO
급여와 근태를 연결한다
반영된 근태를 조회한다

        ↓

SERVICE
이번 월 급여에 사용할 근태를
선별하고 확정해서 급여에 반영한다
```

또:

```text
DTO
기준금액
적용시간
계산금액
계산비고

        ↓

DAO
계산조건과 계산결과를 저장/조회한다

        ↓

SERVICE
기준금액과 근무시간을 이용해
지급액을 계산하고 계산근거를 만든다
```

그래서 네 방식의 핵심은 이거야.

```text
DTO
= 가장 작은 정보

DAO
= 그 정보들을 묶어 DB에서 하는 행동

SERVICE
= 그 DAO 행동들을 묶어 실제 업무 하나를 완성
```

즉 **DTO → DAO는 데이터끼리 결합**, **DAO → Service는 행동끼리 결합**이라고 보면 된다. 이제 미로보드에서 아래에서 위로 올려 그리면 구조가 상당히 자연스럽게 나온다.
